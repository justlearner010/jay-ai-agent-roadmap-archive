# 2026-05-21 作业重新批改

这份批改替代之前过于工程化的版本。昨天是第一天文件读取练习，评价标准应该是“能运行、能解释、能修明显错误”，不是一步到位做成工程化模块。

## 学习阶段

- [x] 入门练习：能运行、能解释、能修明显错误
- [ ] 小步改进：开始拆函数、减少重复、补边界情况
- [ ] 工程化练习：函数、测试、CLI、README

## 总评

评分：85/100  
等级：入门阶段合格

一句话评价：作为第一天的 Python 文件读取练习，你已经完成了核心目标：写出能读取文本文件并统计信息的程序；现在最应该做的是把统计逻辑修准确、把变量名写清楚、确认自己能解释每一行。

## 分项评分

- 当前阶段完成度：27/30
- 正确性和可运行性：20/25
- 代码可读性：12/15
- 学习日志和证据：17/20
- 明日可执行性：9/10

## 必改项

1. 单词数不要用“空格数量 + 1”来算。空文件、多空格、换行都会出错。当前建议先用 `text.split()`。
2. 数字判断不要只写 `"1"` 到 `"9"`，这样会漏掉 `0`。当前建议用 `char.isdigit()`。

## 建议改进项

1. 文件名输入提示写清楚，例如 `请输入文件名：`。
2. 变量名尽量表达意思，例如 `line_count`、`word_count`、`digit_count`。
3. 明天先不用拆复杂模块，可以只尝试把“统计数字数量”这一小段改成一个函数。

## 代码反馈

涉及文件：

- `python-foundations/txt_check.py`

当前阶段做得对的地方：

- 已经使用文件读取。
- 已经能遍历文本内容。
- 已经能输出统计结果。
- 练到了字符串、循环、计数器变量这些基础。

当前阶段不要求：

- 不要求写 `argparse`。
- 不要求拆成 `text_stats.py`。
- 不要求写 pytest。
- 不要求写 README。
- 不要求做成作品集级别结构。

## 明日修改任务

任务 1：用一小段示例文本手动运行 `txt_check.py`，确认行数、单词数、数字数量、字符数是否符合预期。

任务 2：读懂并解释这几行：

```python
lines = text.splitlines()
words = text.split()
char.isdigit()
char.isspace()
```

以后再升级：

- 把统计逻辑拆成函数。
- 给空文件写测试。
- 改成命令行参数。

## 是否建议上传 GitHub

- [x] 可以上传入门练习版
- [ ] 修改后再上传
- [ ] 暂不上传，需要重做关键部分

