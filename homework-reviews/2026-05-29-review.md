# 2026-05-29 作业批改

日期：2026-05-29

归档文件：

- `homework-reviews/2026-05-29-review.md`

归档要求：

- 每次作业批改结果都必须保存到 `homework-reviews/`。
- 文件名使用 `YYYY-MM-DD-review.md`。
- 如果当天有复查结果，可以追加到同一个文件的“复查结果”部分。

学习阶段：

- [ ] 入门练习：能运行、能解释、能修明显错误
- [x] 小步改进：开始拆函数、减少重复、补边界情况
- [x] 工程化练习：函数、测试、CLI、README

## 总评

评分：84/100
等级：良好，可以提交，但要先把 Git 状态整理干净

一句话评价：

今天比昨天明显进步：你补了去重函数和测试，补了 `str_reverse` 边界测试，并把 pytest 配置问题定位到父目录运行场景；当前测试已经能从 `python-foundations` 父目录一次跑通，但代码文件里仍有顶层演示代码和未函数化练习。

## 分项评分

- 当前阶段完成度：26/30
- 正确性和可运行性：23/25
- 代码可读性：11/15
- 学习日志和证据：16/20
- 明日可执行性：8/10

## 必改项

最多 1-2 条，只写当前阶段必须修的内容。

1. 把 `python-foundations/practice/src/newtest.py` 里的顶层演示代码收起来。现在导入 `remove_duplicates` 时会执行多段 `print()` 和演示逻辑，测试虽然通过，但函数文件不够干净。
2. 明确处理并提交 pytest 配置修复：`python-foundations/pytest.ini`、`python-foundations/.vscode/settings.json`、`python-foundations/first-CLI/pytest.ini` 和 `.gitignore` 应该作为一组“修复测试配置”的提交，不要和无关资源文件混在一起。

## 建议改进项

最多 1-3 条，分清楚“明天可以做”和“以后再做”。

1. 明天可以把分组、两数之和、按字段排序也改成函数，并为它们补测试。今天日志写了这 4 个练习，但代码里只有 `remove_duplicates()` 真正函数化并被测试。
2. 明天可以给 `list_max()` 补负数列表和空列表测试，并决定空列表是抛错还是返回 `None`。
3. 以后再把多个小练习拆成更清楚的文件，例如 `dedupe.py`、`group_words.py`、`two_sum.py`、`sort_records.py`。

## 代码反馈

涉及文件：

- `python-foundations/practice/src/newtest.py`
- `python-foundations/practice/tests/test_newtest.py`
- `python-foundations/practice/tests/test_str_reverse.py`
- `python-foundations/practice/tests/test_list_max.py`
- `python-foundations/pytest.ini`
- `python-foundations/.vscode/settings.json`
- `python-foundations/first-CLI/pytest.ini`
- `.gitignore`

具体问题：

- 我从 `python-foundations` 父目录运行了完整测试：`first-CLI/.venv/bin/python -m pytest -q`，结果是 `8 passed`。
- 我从 `practice` 子目录单独运行：`.venv/bin/python -m pytest -q`，结果是 `3 passed`。
- 我从 `first-CLI` 子目录单独运行：`.venv/bin/python -m pytest -q`，结果是 `5 passed`。
- `test_newtest.py` 对 `remove_duplicates()` 覆盖了普通重复、全重复和空列表，这比昨天的测试质量更好。
- `test_str_reverse.py` 增加了空字符串和单字符测试，这是有效的边界补充。
- `newtest.py` 里第 3-63 行仍然是顶层演示代码，导入模块时会自动执行。建议只保留函数，演示代码放到 `if __name__ == "__main__":` 下面，或者放到学习日志。
- `str_reverse.py` 仍然会在函数里 `print(rstr)`。测试函数最好无副作用，只返回结果。
- 你已经把 `_init_.py` 改成了 `__init__.py`，这是对的。
- `.venv/`、`.pytest_cache/`、`__pycache__/` 现在已经被 `.gitignore` 忽略，这一点比昨天更稳。

当前阶段不要求：

- 不要求马上做完整 package 发布。
- 不要求所有练习都写成复杂类或模块。
- 不要求马上加入 coverage。
- 不要求今天把所有旧文件删除和迁移一次性整理完。

## 学习日志反馈

- 今天日志能写出真实进展：Python 练习、pytest 配置、去重、分组、两数之和、排序。
- “如何解决”写到了 `dict.fromkeys()` 和按首字母分组，说明你不是只抄代码，而是在理解数据结构。
- 证据还可以更具体：测试结果不要只写 `pass`，建议写 `8 passed`、`3 passed` 以及运行命令。
- Commit 名称写成 `Add new four test`，但实际 Git 里是 `c08a442 Add four tests as today's homework`，日志最好写准确 commit hash。
- 今天本地分支已经 ahead 2，说明 commit 做了，但还没有推送到 GitHub。

## 明日修改任务

- 任务 1：清理 `newtest.py`，只保留函数；把演示代码移到 `if __name__ == "__main__":` 或学习日志。
- 任务 2：把分组、两数之和、排序三个练习函数化，并各补 1 个最小测试。

以后再升级：

- 给每个练习写 docstring。
- 用参数化测试减少重复。
- 把 `practice` 做成一组清楚的 Python 基础小题库。

## 是否建议上传 GitHub

- [ ] 可以上传
- [x] 修改后再上传
- [ ] 暂不上传，需要重做关键部分

说明：今天作业质量已经可以上传，但当前工作区还有未提交的 pytest 配置修复、本批改文件、旧文件删除和资源文件改动。建议先单独提交“pytest 配置修复 + 今日批改”，再考虑 push；不要把无关的旧文件删除和资源文档混在同一个提交里。

## 复查结果

复查日期：

- [ ] 通过
- [ ] 未通过

复查备注：

