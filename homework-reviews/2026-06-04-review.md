# 每日作业批改

日期：2026-06-04

归档文件：

- `homework-reviews/2026-06-04-review.md`

批改对象：

- `daily-homework/week-03/day-01.md`
- `learning-log/second-week-log/2026-06-04学习日志.md`
- `notes/third-week notes/2026-06-04笔记：python venv.md`
- `notes/third-week notes/2026-06-04笔记：uv使用攻略.md`
- `notes/third-week notes/2026-06-04笔记：installing package pip & uv.md`
- 当前提交：`b64bcb6 Add three notes`

说明：这次批改按用户更正后的任务口径执行，即“第 3 周 Day 1：虚拟环境和包管理”。此前误按第 2 周 Day 4 批改的结论作废。

学习阶段：

- [ ] 入门练习：能运行、能解释、能修明显错误
- [ ] 小步改进：开始拆函数、减少重复、补边界情况
- [x] 工程化练习：函数、测试、CLI、README

## 总评

评分：76/100
等级：合格，有少量修改项

一句话评价：今天的学习内容和 week3 Day1 主线是对齐的，已经围绕 venv、pip、uv、pyproject.toml 留下了笔记和 commit；但还缺少可复现的项目环境证据，例如实际目录、依赖文件、安装/运行命令输出，以及准备提交给 Codex 的具体问题。

## 分项评分

- 当前阶段完成度：22/30
- 正确性和可运行性：17/25
- 代码可读性：10/15
- 学习日志和证据：18/20
- 明日可执行性：9/10

## 必改项

1. 在学习日志或 README 中补上实际运行命令和结果，至少包括：创建或进入虚拟环境、安装/查看依赖、运行测试或运行 CLI、退出环境。
2. 明确写出今天的项目环境落点：到底是继续使用 `python-foundations/first-CLI/.venv`，还是新建一个独立 CLI 项目目录；如果选择 uv，需要留下 `.python-version`、`pyproject.toml` 或等价说明。

## 建议改进项

1. 明天可以先给 `first-CLI` 建一个最小工程化说明：环境创建、依赖安装、运行测试、运行 CLI。
2. 把 1-3 个要问 Codex 的问题写具体，例如：`first-CLI 应该继续用 requirements.txt 还是迁移到 pyproject.toml？`、`src/main.py 现在是否适合拆成 cli.py 和 core.py？`
3. 笔记中把“概念判断”和“命令记录”分开，避免以后复习时不知道哪些是事实命令，哪些是你的阶段性理解。

## 代码反馈

涉及文件：

- `python-foundations/first-CLI/`
- `python-foundations/practice/`
- `notes/third-week notes/2026-06-04笔记：python venv.md`
- `notes/third-week notes/2026-06-04笔记：uv使用攻略.md`
- `notes/third-week notes/2026-06-04笔记：installing package pip & uv.md`

具体问题：

- 仓库里已经存在两个虚拟环境目录：`python-foundations/first-CLI/.venv` 和 `python-foundations/practice/.venv`，这能证明你确实在使用项目隔离环境。
- 当前没有找到 `requirements.txt`、`pyproject.toml`、`.python-version` 或 `uv.lock`，所以“记录当前依赖 / 使用 uv 工程化”的证据还不完整。
- 当前 `HEAD` 提交 `b64bcb6 Add three notes` 只包含 3 份笔记，没有包含项目依赖文件或 CLI 目录调整。
- `python-foundations` 全量测试仍然通过：`23 passed in 0.04s`，已有代码没有被破坏。
- `git diff --check` 仍提示 `python-foundations/first-CLI/src/main.py` 第 24 行和第 31 行有尾随空格，后续提交前应清理。

当前阶段不要求：

- 不要求今天完成完整 CLI 文本总结器。
- 不要求立刻接入真实 LLM。
- 不要求一次性迁移整个仓库到 uv。
- 不要求把所有旧 Python 练习重新整理成标准 package。

## 学习日志反馈

- 日志主题和 week3 Day1 对齐：venv、pip、uv、pyproject.toml 都是“虚拟环境和包管理”的核心内容。
- “代码文件：无”和“测试结果：无”让证据偏弱。Day1 可以没有新业务代码，但至少应该有命令证据，例如 `python -m venv .venv`、`source .venv/bin/activate`、`pip list`、`python -m pytest ...`、`deactivate`。
- “今天卡点：无”不建议这样写。今天至少可以记录 1 个判断问题：`requirements.txt 和 pyproject.toml 什么时候分别适合？` 或 `first-CLI 是否应该独立成一个 uv 项目？`
- 笔记里有几处需要修正：`deactive` 应为 `deactivate`；`pip install request` 通常应为 `pip install requests`；`uv python uv install cpython-3.12` 这条命令需要重新核对。
- “pip、venv、requirements.txt 是过时写法”这个判断太绝对。更准确：它们仍然常见且可用；uv、Poetry、PDM 更适合在复杂项目里统一环境、依赖和锁文件。

## 明日修改任务

- 任务 1：在 `python-foundations/first-CLI/README.md` 或 `python-foundations/README.md` 补一节“环境和运行方式”，写清楚创建环境、进入环境、安装依赖、运行测试、退出环境的命令。
- 任务 2：选择一个依赖记录方式：如果继续简单路线，新增 `requirements.txt`；如果走 uv 路线，新增最小 `pyproject.toml` 和 `.python-version`。
- 任务 3：把明天要改的 CLI 骨架写具体：例如先整理 `python-foundations/first-CLI/src/main.py` 的入口函数和参数说明，不要同时重构所有文件。

以后再升级：

- 把 `first-CLI` 单独整理成可运行的 CLI 文本总结器骨架。
- 使用 mock LLM 函数生成固定摘要结果。
- 给 CLI 参数、文件输入输出、错误提示补测试。

## 是否建议上传 GitHub

- [ ] 可以上传
- [x] 修改后再上传
- [ ] 暂不上传，需要重做关键部分

说明：今天 3 份笔记本身可以保留，也已经进入本地提交 `b64bcb6 Add three notes`。但如果作为 week3 Day1 的完整作业，建议先补依赖文件或环境说明、运行命令结果和 1-3 个具体问题，再连同本批改文件一起上传。

## 复查结果

复查日期：

- [ ] 通过
- [ ] 未通过

复查备注：
