# 每日作业批改

日期：2026-05-31

归档文件：

- `homework-reviews/2026-05-31-review.md`

学习阶段：

- [ ] 入门练习：能运行、能解释、能修明显错误
- [x] 小步改进：开始拆函数、减少重复、补边界情况
- [x] 工程化练习：函数、测试、CLI、README

## 总评

评分：76/100
等级：合格，有明确修改项

一句话评价：今天把昨天的关键问题推进了，`main.py` 已经改成类调用，`TextStats` 也补了测试，全量 pytest 已经通过；但第 1 周 Day 7 的周复盘和追踪表闭环还不完整，CLI 对 `chunk_size=0` 仍会崩。

## 分项评分

- 当前阶段完成度：22/30
- 正确性和可运行性：21/25
- 代码可读性：11/15
- 学习日志和证据：14/20
- 明日可执行性：8/10

## 必改项

1. 补完整第 1 周复盘和追踪表：`learning-log/weekly_review/第一周复盘.md` 需要写清原计划交付物、测试命令/结果、下周计划；`trackers/weekly-log.csv` 和 `trackers/homework-review-log.csv` 还没有填入本周实际结果。
2. 修复 `Wordchunk.text_chunk()` 的 `chunk_size == 0` 边界。现在负数会返回 `-1`，但 0 会触发 `ValueError: range() arg 3 must not be zero`。

## 建议改进项

1. `main.py` 的文件存在性检查现在会打印 `0`，这更像调试残留；建议去掉，或者换成清楚的成功/错误提示。
2. `create_json.py` 里的 `FileNotFoundError` 捕获意义不大，因为写入 `test.json` 通常会创建文件；更值得处理的是输出路径、权限或 JSON 序列化失败。
3. `convertError.py` 和 `void_input.py` 是有效的异常练习，但它们在顶层直接 `input()`，后续如果要测试，最好改成函数形式。

## 代码反馈

涉及文件：

- `python-foundations/first-CLI/src/main.py`
- `python-foundations/first-CLI/src/create_json.py`
- `python-foundations/first-CLI/src/word_chunk.py`
- `python-foundations/first-CLI/tests/test_text_status.py`
- `python-foundations/practice/src/convertError.py`
- `python-foundations/practice/src/void_input.py`
- `learning-log/weekly_review/第一周复盘.md`
- `trackers/weekly-log.csv`
- `trackers/homework-review-log.csv`

具体问题：

- `main.py` 已经正确使用 `TextStats(fname)` 和 `Wordchunk(fname, chunk_size)`，昨天的旧导入问题修掉了。
- `test_text_status.py` 覆盖了行数、词数、数字数和空白字符数，方向正确。
- `test_text_status.py` 只有一个综合测试；下一步可以补空文件或只有空白字符的边界测试。
- `word_chunk.py` 需要把 `chunk_size <= 0` 统一当作非法输入处理。
- 周复盘目前还偏短，尤其缺少“下周计划”和具体测试证据，不符合 Day 7 的主要目标。

当前阶段不要求：

- 不要求把异常练习都写成完整项目。
- 不要求给所有 CLI 分支都补测试。
- 不要求现在重构整个目录结构。

## 测试结果

我实际运行了：

```bash
python-foundations/first-CLI/.venv/bin/python -m pytest python-foundations/first-CLI/tests -q
```

结果：

```text
6 passed in 0.06s
```

我实际运行了：

```bash
python-foundations/practice/.venv/bin/python -m pytest python-foundations/practice/tests -q
```

结果：

```text
6 passed in 0.03s
```

我实际运行了：

```bash
python-foundations/practice/.venv/bin/python -m pytest python-foundations -q
```

结果：

```text
12 passed in 0.09s
```

我还用临时目录实际跑了 CLI 主入口，正常输入可以运行成功，并生成 JSON 输出。但 `--chunk_size 0` 会失败：

```text
ValueError: range() arg 3 must not be zero
```

## 学习日志反馈

- 今天日志能说明主线：学习异常处理、修昨天作业、补 `text_status.py` 测试。
- 异常处理笔记里已经能区分 `try`、`except`、`raise`，这个概念对后续 CLI 和 API 错误处理有直接价值。
- 日志里的“测试结果：pass”建议以后写成具体命令和输出，例如 `12 passed in 0.09s`，这样证据更可复查。
- Day 7 是周日复盘日，今天日志还没有把“本周完成度”和“下周具体任务”讲清楚。

## 明日修改任务

- 任务 1：补完整 `learning-log/weekly_review/第一周复盘.md`，至少写清本周 3 个最重要函数、测试命令和下周 3 个具体任务。
- 任务 2：更新 `trackers/weekly-log.csv` 和 `trackers/homework-review-log.csv` 的第一周记录。
- 任务 3：修 `Wordchunk.text_chunk()` 的 `chunk_size <= 0` 输入，并补一个对应测试。

以后再升级：

- 把 `convertError.py`、`void_input.py` 改成函数 + 测试。
- 给 `TextStats` 增加空文件、纯空白文本等边界测试。
- 清理工作区里大量移动/删除后的 Git 状态，提交前不要直接 `git add .`。

## 是否建议上传 GitHub

- [ ] 可以上传
- [x] 修改后再上传
- [ ] 暂不上传，需要重做关键部分

说明：当前本地 `main` 已经 ahead 4，代码测试已全绿，但 Day 7 的复盘和追踪表还没完成，工作区也有大量未提交/未跟踪文件。建议先补复盘和追踪表，再按主题分组提交。

## 复查结果

复查日期：

- [ ] 通过
- [ ] 未通过

复查备注：
