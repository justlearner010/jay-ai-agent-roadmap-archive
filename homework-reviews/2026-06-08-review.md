# 每日作业批改

日期：2026-06-08

归档文件：

- `homework-reviews/2026-06-08-review.md`

批改对象：

- `daily-homework/week-03/day-05.md`
- `python-foundations/first-CLI/src/main.py`
- `python-foundations/first-CLI/src/cli.py`
- `python-foundations/first-CLI/src/create_json.py`
- `learning-log/third-week-log/2026-06-08日志：.md`
- `notes/third-week notes/2026-06-08笔记：pathlib.md`
- `notes/third-week notes/2026-06-08笔记 logging的实际用法.md`

学习阶段：

- [ ] 入门练习：能运行、能解释、能修明显错误
- [ ] 小步改进：开始拆函数、减少重复、补边界情况
- [x] 工程化练习：函数、测试、CLI、README

## 总评

评分：48/100
等级：未通过，先修关键功能

一句话评价：今天做了 `main()` 和 `cli.py` 拆分，也学习了 pathlib 和 logging 的实际用法，但 Day5 的核心交付没有完成；更严重的是 `main.py` 入口判断写错，导致 CLI 命令实际不会执行。

## 分项评分

- 当前阶段完成度：12/30
- 正确性和可运行性：7/25
- 代码可读性：11/15
- 学习日志和证据：13/20
- 明日可执行性：7/10

## 必改项

最多 1-2 条，只写当前阶段必须修的内容。

1. 修复 `python-foundations/first-CLI/src/main.py` 第 85 行：`if __name__ == "__main":` 少了结尾两个下划线，正确写法是 `if __name__ == "__main__":`。当前命令运行没有输出，就是因为 `main()` 没有被调用。
2. 重做 Day5 JSON 输出：`--createjson` 不能只输出 chunks，必须输出包含 `summary`、`mode`、`input_chars` 的结构化结果，并用 `json.load()` 或 `json.loads()` 验证可以重新读取。

## 建议改进项

最多 1-3 条，分清楚“明天可以做”和“以后再做”。

1. 明天可以在 `create_json.py` 里先创建输出目录：`output_path.parent.mkdir(parents=True, exist_ok=True)`，否则 `output/` 不存在时写文件会失败。
2. 明天可以给 `create_json()` 写一个最小测试：传入 summary 结果，写出 JSON，再读取回来断言字段存在。
3. 以后再考虑把 `summary_mode` 做成 CLI 参数，例如 `--summary-mode brief|bullets`。

## 代码反馈

涉及文件：

- `python-foundations/first-CLI/src/main.py`
- `python-foundations/first-CLI/src/cli.py`
- `python-foundations/first-CLI/src/create_json.py`

具体问题：

- `main.py` 第 13 行开始拆出 `main()` 是正确方向，`cli.py` 也让 argparse 配置更清楚。
- `main.py` 第 85 行入口判断写错，导致 `./.venv/bin/python src/main.py test0.txt --summary` 无输出、无日志、无真正执行结果。这是今天最大问题。
- `create_json.py` 第 5 行把输出路径改成 `output/chunks.json`，但第 10 行直接 open，没有确保 `output/` 目录存在。
- `create_json.py` 当前仍然把 `chunks` 直接 dump 出去，不符合 Day5 的 `summary`、`mode`、`input_chars` 要求。
- `main.py` 第 27 行仍写 `output_path = "test.json"`，但 `create_json.py` 实际写的是 `output/chunks.json`，日志里的输出路径和真实路径不一致。
- `main.py` 第 64-67 行 summary 结果只是打印和 logging，没有进入 JSON 结果。

验证结果：

- `./.venv/bin/python -m pytest tests -q`：`11 passed in 0.06s`
- `./.venv/bin/python src/main.py test0.txt --summary`：命令返回 0，但没有任何输出；原因是 `if __name__ == "__main":` 写错，`main()` 未执行。
- `rm -rf output && ./.venv/bin/python src/main.py test0.txt --createjson`：命令返回 0，但实际没有创建 `output/chunks.json`；同样因为 `main()` 未执行。
- `mkdir -p output && ./.venv/bin/python src/main.py test0.txt --createjson && ./.venv/bin/python -m json.tool output/chunks.json`：读取失败，`output/chunks.json` 不存在。

当前阶段不要求：

- 不要求接真实 LLM。
- 不要求接前端或 API。
- 不要求做复杂配置系统。
- 不要求一次性支持多种 JSON schema。

## 学习日志反馈

- 学习日志写到了今天完成 `main.py` 工程化，并拆分 `argparse` 到 `cli.py`，这是真实进展。
- 卡点写得准确：不知道如何用 pathlib 更改 JSON 创建目录；这个问题正好对应 `output_path.parent.mkdir(parents=True, exist_ok=True)`。
- 证据部分没有填测试结果，不能支撑提交。
- Commit 写了 `add cli.py`，并且本地最近 commit 确实是 `77a040a add cli.py`，但今天 Day5 的 JSON 输出目标还没有达成。

## 笔记反馈

- pathlib 笔记覆盖了路径拼接、读写文件、创建目录、遍历目录，内容方向对。
- logging 实际用法笔记比昨天更成熟，尤其是“不要每个模块都写 basicConfig，应该在 main.py 统一配置”这个判断是对的。
- logging 笔记里的 `logging.basicConfig(...)` 示例少了逗号：`level = logging.INFO` 后面需要 `,`，否则复制运行会语法错误。

## 明日修改任务

- 任务 1：打开 `python-foundations/first-CLI/src/main.py`，把第 85 行改成 `if __name__ == "__main__":`，重新运行 `--summary` 确认有输出。
- 任务 2：打开 `python-foundations/first-CLI/src/create_json.py`，让输出 JSON 至少包含 `summary`、`mode`、`input_chars`，并在写文件前创建 `output/` 目录。
- 任务 3：新增或手动记录一个 JSON 读取验证：写完后用 `json.load()` 读回文件，确认字段存在。

以后再升级：

- 给 CLI 主入口补 subprocess 级别测试，避免入口拼写错误但 pytest 仍通过。
- 把 `create_json()` 的输出路径作为参数传入。
- 统一 `print` 输出和 JSON 输出的数据结构，减少重复计算。

## 是否建议上传 GitHub

- [ ] 可以上传
- [ ] 修改后再上传
- [x] 暂不上传，需要重做关键部分

说明：今天代码有工程化进展，但 CLI 入口没有真正执行，Day5 JSON 结构也未完成。先修入口和 JSON schema，再复查。

## 复查结果

复查日期：

- [ ] 通过
- [x] 未通过

复查备注：

## 复查结果：2026-06-08 第一次复查

复查评分：64/100
等级：未通过，但关键入口问题已修

一句话评价：`main.py` 的入口判断已经修好，CLI 现在能真正执行；`output/` 目录创建也已修好，JSON 文件可以写出和读回。但 Day5 要求的 JSON 结构仍未完成，当前输出仍是 chunks 列表，而不是包含 `summary`、`mode`、`input_chars` 的结构化结果。

已修复：

- `python-foundations/first-CLI/src/main.py` 入口已改成 `if __name__ == "__main__":`。
- `./.venv/bin/python src/main.py test0.txt --summary` 现在能输出 summary 和日志。
- `python-foundations/first-CLI/src/create_json.py` 已使用 `output_path.parent.mkdir(parents=True, exist_ok=True)` 创建输出目录。
- `./.venv/bin/python src/main.py test0.txt --createjson` 能生成 `output/test0.json`。
- `output/test0.json` 可以用 `json.loads(...)` 读回。

仍未通过：

1. `output/test0.json` 当前读回结果是 list：

```python
[
    {
        "chunk index": 1,
        "chunk_start": 0,
        "chunk_end": 6,
        "chunk_text": "abcdef"
    }
]
```

但 Day5 要求是至少包含：

```python
{
    "summary": "...",
    "mode": "brief",
    "input_chars": 6
}
```

2. `main.py` 里 `logger.info("JSON output created. output_path=%s", output_path)` 仍然记录的是旧的 `test.json`，但真实输出路径已经是 `output/test0.json`，日志和事实不一致。

复查验证：

- `./.venv/bin/python -m pytest tests -q`：`11 passed in 0.04s`
- `./.venv/bin/python src/main.py test0.txt --summary`：通过，能输出 `Brief summary: this text has 1 words`
- `rm -rf output && ./.venv/bin/python src/main.py test0.txt --createjson`：通过，能创建 `output/test0.json`
- JSON 读回：通过，但类型是 `list`，缺少 `summary`、`mode`、`input_chars`

下一次复查只看一件事：

- `output/test0.json` 读回后必须是 dict，并且 `summary`、`mode`、`input_chars` 三个字段都存在。

## 复查结果：2026-06-08 第二次复查

复查评分：82/100
等级：合格，可以上传前再补学习日志证据

一句话评价：Day5 的两个关键问题已经修复：CLI 能真正执行，`--createjson` 现在能写出包含 `summary`、`mode`、`input_chars` 的 JSON，并且可以重新读回验证。

已通过：

- `./.venv/bin/python -m pytest tests -q`：`11 passed in 0.02s`
- `./.venv/bin/python src/main.py test0.txt --summary`：能输出 `Brief summary: this text has 1 words`
- `rm -rf output && ./.venv/bin/python src/main.py test0.txt --createjson`：能创建 `output/test0.json`
- 读回 `output/test0.json` 后类型是 `dict`
- JSON keys 是 `['input_chars', 'mode', 'summary']`
- `has_day5_keys=True`

当前 JSON 结果：

```python
{
    "summary": "Brief summary: this text has 1 words",
    "mode": "brief",
    "input_chars": 6
}
```

仍建议补充：

1. `learning-log/third-week-log/2026-06-08日志：.md` 的“测试结果”仍是空的，建议补上本次三条验证命令和结果。
2. 可以给 `create_json()` 加一个最小测试，避免以后又退回 chunks list。
3. `main.py` 里还有未使用的 `chunks` 计算；现在不影响 Day5 合格，但后续可以按参数只执行需要的功能。

是否建议上传 GitHub：

- [x] 可以上传
- [ ] 修改后再上传
- [ ] 暂不上传，需要重做关键部分

说明：代码功能已经达到 Day5 核心验收标准。上传前最好把学习日志的测试结果补齐，这样作业证据更完整。
