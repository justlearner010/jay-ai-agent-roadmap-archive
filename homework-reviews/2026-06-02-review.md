# 每日作业批改

日期：2026-06-02

归档文件：

- `homework-reviews/2026-06-02-review.md`

学习阶段：

- [ ] 入门练习：能运行、能解释、能修明显错误
- [x] 小步改进：开始拆函数、减少重复、补边界情况
- [x] 工程化练习：函数、测试、CLI、README

## 总评

评分：67/100
等级：勉强合格，第二天必须先补齐任务对齐

一句话评价：今天 pytest 数量和测试通过率有进步，全量已经到 `21 passed`；但第 2 周 Day 2 的核心是去重、分组、两数之和、按字段排序四个数据结构练习，目前只有排序和部分去重比较完整，两数之和还是空文件，分组没有返回值也没有测试。

## 分项评分

- 当前阶段完成度：15/30
- 正确性和可运行性：21/25
- 代码可读性：10/15
- 学习日志和证据：13/20
- 明日可执行性：8/10

## 必改项

1. 补齐 Day 2 的四个指定练习：`two_sum.py` 现在是空文件；`groups_words.py` 只 `print(groups)`，没有 `return groups`，也没有测试。
2. 修正 `list_max([])` 的测试和实现：测试函数写成了 `empty_list`，pytest 不会收集；实现里先执行 `max(list)`，所以空列表会先由 `max()` 抛错，你自己的 `raise ValueError("没有值")` 实际不会执行。

## 建议改进项

1. 日志里写的是 `with pytest.raise(ValueError)`，正确 API 是 `with pytest.raises(ValueError):`。
2. `list_max(list)` 的参数名不要叫 `list`，它会遮蔽 Python 内置类型 `list`；建议改成 `numbers`。
3. `main.py` 仍有尾随空格，`git diff --check` 会报错。提交前需要清理。

## 代码反馈

涉及文件：

- `python-foundations/practice/src/dedupe.py`
- `python-foundations/practice/src/groups_words.py`
- `python-foundations/practice/src/two_sum.py`
- `python-foundations/practice/src/sort_records.py`
- `python-foundations/practice/src/list_max.py`
- `python-foundations/practice/tests/test_list_max.py`
- `python-foundations/practice/tests/test_sort_records.py`
- `python-foundations/practice/tests/test_void_input.py`
- `learning-log/second-week-log/2026-06-02学习日志.md`

具体问题：

- `sort_records.py` 是今天质量最高的部分：`sort_students(students, field, reverse=False)` 已经函数化，测试覆盖了按分数升序、降序、按姓名排序、空列表和缺字段。
- `dedupe.py` 有函数和测试，但空列表行为是抛 `ValueError`，这个可以接受，不过建议在日志里解释为什么空列表不是合法输入。
- `groups_words.py` 目前无法被断言测试，因为函数只打印结果，不返回结果。
- `two_sum.py` 没有内容，Day 2 要求“两数之和不用双重循环也能实现”，这项没有完成。
- `void_input.py` 和 `list_max.py` 是异常/边界测试练习，有价值，但不是 Day 2 的四个指定数据结构练习主线。

当前阶段不要求：

- 不要求今天写复杂复杂度分析。
- 不要求把所有旧练习都一次性重构。
- 不要求把所有函数都加类型标注。

## 测试结果

我实际运行了：

```bash
python-foundations/first-CLI/.venv/bin/python -m pytest python-foundations/first-CLI/tests -q
```

结果：

```text
8 passed in 0.07s
```

我实际运行了：

```bash
python-foundations/practice/.venv/bin/python -m pytest python-foundations/practice/tests -q
```

结果：

```text
13 passed in 0.04s
```

我实际运行了：

```bash
python-foundations/practice/.venv/bin/python -m pytest python-foundations -q
```

结果：

```text
21 passed in 0.11s
```

但我也确认了：

```bash
python-foundations/practice/.venv/bin/python -m pytest python-foundations/practice/tests/test_list_max.py --collect-only -q
```

只收集到：

```text
tests/test_list_max.py::test_list_max
```

也就是说 `empty_list()` 没有被 pytest 执行。

我还手动检查 `list_max([])`：

```text
ValueError max() iterable argument is empty
```

这说明当前异常来自 `max()`，不是你自己写的 `raise ValueError("没有值")`。

## 学习日志反馈

- 日志记录了今天的真实卡点：不知道如何测试 `ValueError`，这个问题抓得对。
- 日志太短，没有说明 Day 2 的四个练习分别完成到哪里，也没有写 list 查找和 dict 查找的直觉差异。
- “测试结果：pass”建议改成具体命令和输出，例如 `21 passed in 0.11s`。
- 今天没有记录算法微练习或自由探索；如果当天主线已经很重，可以写“今天跳过”，这样记录更清楚。

## 明日修改任务

- 任务 1：完成 `two_sum.py`，用 dict 做查找，并补“找到”和“找不到”两个测试。
- 任务 2：修改 `groups_words.py`，让 `words_group(words)` 返回字典，并补一个测试。
- 任务 3：把 `empty_list` 改名为 `test_empty_list`，并把 `list_max` 的空列表判断放到 `max()` 之前。

以后再升级：

- 给四个数据结构练习补类型标注。
- 把 `dedupe.py` 顶层演示代码移到测试或 `if __name__ == "__main__":` 下面。
- 清理 `main.py` 尾随空格和工作区大量未提交/未跟踪文件。

## 是否建议上传 GitHub

- [ ] 可以上传
- [x] 修改后再上传
- [ ] 暂不上传，需要重做关键部分

说明：测试全绿，但任务对齐不够，且 `list_max` 的边界测试没有被收集。建议先补齐 `two_sum`、`groups_words` 和 `list_max` 的问题，再提交。

## 复查结果

复查日期：

- [ ] 通过
- [ ] 未通过

复查备注：
