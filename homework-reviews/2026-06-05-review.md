# 每日作业批改

日期：2026-06-05

归档文件：

- `homework-reviews/2026-06-05-review.md`

批改对象：

- `daily-homework/week-03/day-02.md`
- `learning-log/third-week-log/2026-06-05日志.md`
- `notes/third-week notes/2026-06-05笔记：如何写pyproject.toml.md`
- `python-foundations/first-CLI/src/main.py`
- `python-foundations/first-CLI/src/word_chunk.py`
- `python-foundations/first-CLI/src/text_status.py`
- `python-foundations/first-CLI/src/word_freq.py`
- `python-foundations/first-CLI/tests/test_word_chunk.py`
- `python-foundations/first-CLI/tests/test_text_status.py`
- `python-foundations/first-CLI/tests/test_word_freq.py`
- `python-foundations/first-CLI/pyproject.toml`
- `python-foundations/README.md`

说明：学习日志写的是 `Week3 Day2`，实际内容也围绕 CLI 参数设计、summary mode 和工程化配置；因此本次按“第 3 周 Day 2：CLI 参数设计”批改。仓库里的 `daily-homework/week-03/day-02.md` 日期是 2026-06-09，和今天 2026-06-05 不一致，这里按用户当天实际提交内容处理。

学习阶段：

- [ ] 入门练习：能运行、能解释、能修明显错误
- [ ] 小步改进：开始拆函数、减少重复、补边界情况
- [x] 工程化练习：函数、测试、CLI、README

## 总评

评分：66/100
等级：勉强合格，明天必须先修改

一句话评价：今天你确实在推进 `first-CLI` 的工程化，并补了 `pyproject.toml` 笔记和 8 个通过的单元测试；但 CLI 主入口现在不能正常运行，`--summary` 参数也不能被识别，所以当天核心目标“CLI 参数设计”还没有形成可用闭环。

## 分项评分

- 当前阶段完成度：18/30
- 正确性和可运行性：12/25
- 代码可读性：10/15
- 学习日志和证据：16/20
- 明日可执行性：10/10

## 必改项

最多 1-2 条，只写当前阶段必须修的内容。

1. 修复 `python-foundations/first-CLI/src/main.py` 的 argparse 顺序问题：必须先添加所有参数，再执行 `parser.parse_args()`。当前 `--summary` 在 `parse_args()` 之后才添加，导致 `--summary` 无法识别；即使不传 `--summary`，最后访问 `args.summary` 也会抛 `AttributeError`。
2. 给 `main.py` 补一条最小运行证据：至少记录普通模式和 summary mode 各一条命令及输出。单元测试通过还不够，因为今天的核心是 CLI 入口能不能跑。

## 建议改进项

最多 1-3 条，分清楚“明天可以做”和“以后再做”。

1. 明天可以把 `main.py` 里的顶层执行逻辑收进 `main()` 函数，并用 `if __name__ == "__main__": main()` 调用，避免以后测试 CLI 时一 import 就执行。
2. 明天可以把 `--chunk_size` 的 help 从 `Words per chunk` 改成更准确的 `Characters per chunk`，因为 `Wordchunk.text_chunk()` 当前按字符长度切块，不是按单词数切块。
3. 以后再处理 `pyproject.toml` 的项目元数据：`authors` 仍是占位值，`license = "MIT lisense"` 有拼写错误，`requires-python = ">=3.14"` 对展示项目来说门槛偏高。

## 代码反馈

涉及文件：

- `python-foundations/first-CLI/src/main.py`
- `python-foundations/first-CLI/src/word_chunk.py`
- `python-foundations/first-CLI/tests/test_word_chunk.py`
- `python-foundations/first-CLI/pyproject.toml`
- `python-foundations/README.md`

具体问题：

- 实际运行 `python-foundations/first-CLI/.venv/bin/python -m pytest python-foundations/first-CLI -q`，结果是 `8 passed in 0.02s`。这说明核心函数测试有进步，尤其是 `word_chunk`、`text_status`、`word_freq` 都有可检查证据。
- 实际运行 `python-foundations/first-CLI/.venv/bin/python python-foundations/first-CLI/src/main.py python-foundations/first-CLI/test0.txt --summary` 失败，报错为 `unrecognized arguments: --summary`。
- 实际运行 `python-foundations/first-CLI/.venv/bin/python python-foundations/first-CLI/src/main.py python-foundations/first-CLI/test0.txt --chunk_size 2` 也失败，最后报 `AttributeError: 'Namespace' object has no attribute 'summary'`。
- `word_chunk.py` 对负数 chunk size 做了检查，但对 `0` 没有处理；现在 `main.py` 已经提前拦截 `chunk_size <= 0`，短期可以接受。以后如果 `Wordchunk` 会被单独调用，类内部也应该统一处理 `<= 0`。
- `python-foundations/README.md` 已经列出了 `first-CLI` 和 `practice` 的练习索引，这是符合 Day 5 目录整理要求的；但作为项目 README，还缺运行命令和测试命令。
- 安全检查没有发现 API key、secret、token、绝对路径泄露；`.venv/`、`.pytest_cache/`、`__pycache__/` 当前被 `.gitignore` 忽略。

当前阶段不要求：

- 不要求今天接入真实 LLM。
- 不要求把 CLI 完整打包成可安装命令。
- 不要求一次性重构所有模块。
- 不要求现在完成语义切块。

## 学习日志反馈

- 三行日志符合新的轻量模板：今天完成、卡点、明天第一步都有写。
- “今天完成”写到了 summary mode 和 `--help`，但测试结果仍是空的。今天实际可以补：`python-foundations/first-CLI: 8 passed`，同时记录 CLI 运行失败，这也是有效证据。
- “明天第一步”方向是对的，但可以更具体：先改 `src/main.py` 中 `parse_args()` 的位置，再运行普通模式和 summary mode。
- `pyproject.toml` 笔记能说明概念，但有几处表达要收紧：它不只是“告诉包管理器下载什么”，还负责项目元数据、构建系统、工具配置和依赖声明；`requirements.txt` 没有被完全替代，简单项目仍然常用。

## 明日修改任务

- 任务 1：在 `python-foundations/first-CLI/src/main.py` 里把 `parser.parse_args()` 移到所有 `add_argument()` 之后。
- 任务 2：运行并记录两条命令：普通模式 `python .../main.py test0.txt --chunk_size 2`，summary mode `python .../main.py test0.txt --summary`。
- 任务 3：把学习日志里的“测试结果”补成真实结果：包括 `8 passed` 和 CLI 修复后的输出。

以后再升级：

- 把 `main.py` 整理成 `main()` 函数。
- 给 CLI 参数解析补测试。
- 给 README 增加安装、运行、测试、示例输出。
- 统一 `pyproject.toml`、`requirements.txt`、README 中的依赖管理说明。

## 是否建议上传 GitHub

- [ ] 可以上传
- [x] 修改后再上传
- [ ] 暂不上传，需要重做关键部分

说明：单元测试是通过的，学习日志和笔记也值得保留；但 CLI 主入口当前会失败，而今天的核心目标就是 CLI 参数设计。因此建议先修 `main.py`，补普通模式和 summary mode 的运行证据，再上传。

## 复查结果

复查日期：

- [ ] 通过
- [ ] 未通过

复查备注：
