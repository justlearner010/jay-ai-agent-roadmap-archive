# 2026-05-24 作业批改

日期：2026-05-24

归档文件：

- `homework-reviews/2026-05-24-review.md`

## 学习阶段

- [ ] 入门练习：能运行、能解释、能修明显错误
- [x] 小步改进：开始拆函数、减少重复、补边界情况
- [x] 工程化练习：函数、测试、CLI、README

说明：今天已经开始接触命令行参数、JSON 输出和 chunking，属于从“小步改进”向“工程化练习”过渡，不要求一次做到完整工程化。

## 总评

评分：85/100  
等级：良好，代码能力在进步，但仓库整理需要马上收住

一句话评价：今天的核心作业 `word_chunk.py` 是有效进步：你开始使用 `sys.argv` 接收参数、用 `json.dump()` 写文件，并把 RAG 里的 chunking 概念落到了代码里；主要问题不是方向，而是输出文件路径、参数检查和仓库里未提交/误删文件较多，需要先整理证据链。

## 分项评分

- 当前阶段完成度：27/30
- 正确性和可运行性：21/25
- 代码可读性：11/15
- 学习日志和证据：17/20
- 明日可执行性：9/10

## 必改项

最多 1-2 条，只写当前阶段必须修的内容。

1. `word_chunk.py` 需要处理参数不足的情况。现在如果直接运行 `python3 python-foundations/word_chunk.py`，会因为 `sys.argv[1]` 或 `sys.argv[2]` 不存在而报错。明天先加一个最简单的提示即可。
2. 先整理仓库状态。当前有多个根目录文档显示为删除，还有 `.DS_Store`、`.obsidian/`、截图等未跟踪文件。不要直接全部提交，先确认哪些是学习证据，哪些不该进 GitHub。

## 建议改进项

最多 1-3 条，分清楚“明天可以做”和“以后再做”。

1. 明天可以把 `chunk index` 改成真正的序号，例如 0、1、2，而不是现在的字符起点 0、10、20。
2. 明天可以把输出文件名作为第三个命令行参数传入，避免每次都写死成 `test.json`。
3. 以后再学 `argparse`，现在先不用急；`sys.argv` 先掌握清楚就够。

## 代码反馈

涉及文件：

- `python-foundations/word_chunk.py`
- `python-foundations/word_frequency_count.py`
- `python-foundations/text_status.py`

做得好的地方：

- `word_chunk.py` 能按固定字符长度切分文本。
- 已经开始用列表保存多个 chunk，每个 chunk 用字典保存结构化信息。
- 已经用 `json.dump(..., ensure_ascii=False, indent=4)` 写出可读 JSON，这对后面的 RAG 数据处理很有价值。
- `word_frequency_count.py` 的 Counter 版本已经能输出真正的 Top 10，说明你修正了前一天“只取前 10 个出现词”的问题。
- 今天已经有提交记录：`bc4d2ec Add my 2026-05-24 homework`，并且本地 `main` 与 `origin/main` 同步。

具体问题：

- `word_chunk.py` 里 `j_txt = json.dumps(...)` 没有被使用，可以删掉。
- `chucks` 应该改成 `chunks`，这是拼写问题，不影响运行但影响阅读。
- `chunk_end` 最后一段会超过文本长度，例如文本只剩 8 个字符时仍显示 `i + chunk_size`，后面可以用 `min(i + chunk_size, len(text))`。
- `create_json()` 写死输出到当前工作目录的 `test.json`。从不同目录运行时，文件会生成到不同地方，容易混乱。
- 打开文件建议逐步加上 `encoding="utf-8"`，避免中文文件以后出问题。

当前阶段不要求：

- 不要求 pytest。
- 不要求语义 chunking。
- 不要求一次做 overlap。
- 不要求马上学完整 `argparse`。
- 不要求今天就把 JSON、RAG、chunking 全部做成项目级代码。

## 学习日志反馈

今天日志比前几天更扎实，优点很明确：

- 目标写清楚了：文本分块、LLM app 基础、JSON。
- 遇到的问题是真问题：不会生成 JSON、不会不用 `input()` 传参数、不会创建 JSON 文件。
- 解决方式能对应到具体知识：`json.dump()`、`json.dumps()`、`sys.argv`、`with open(..., "w")`。
- 另外写了 JSON、RAG chunk、字符串方法三篇笔记，说明今天不是只写了代码，也补了概念。

需要改进：

- “测试结果：成功”仍然太笼统。建议写成实际命令，例如：

```bash
python3 python-foundations/word_chunk.py python-foundations/01.txt 500
```

- 日志里写 “Commit：成功” 是对的，但最好补 commit id，例如 `bc4d2ec`。
- 图片仍然是 Obsidian 语法 `![[...]]`，GitHub 不能正常显示。要么移动到 `learning-log/assets/` 后改成标准 Markdown 图片链接，要么先不放截图。

## 明日修改任务

- 任务 1：给 `word_chunk.py` 加最简单的参数数量检查。如果参数不足，打印用法提示并退出。
- 任务 2：整理仓库状态，只保留应该提交的学习材料；`.DS_Store`、`.obsidian/` 和临时截图先不要提交。

以后再升级：

- 给 chunk 加 overlap。
- 支持按段落切分。
- 让输出文件路径可配置。
- 把 chunking 逻辑改成一个返回值清楚的函数，后面再测试。

## 是否建议上传 GitHub

- [ ] 可以上传
- [x] 修改后再上传
- [ ] 暂不上传，需要重做关键部分

说明：今天的 commit 已经上传过了；但当前工作区又出现了大量未提交改动和疑似误删文件，不建议现在直接 `git add . && git commit`。先整理再提交。

## 复查结果

复查日期：

- [ ] 通过
- [ ] 未通过

复查备注：

