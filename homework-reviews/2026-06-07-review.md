# 每日作业批改

日期：2026-06-07

归档文件：

- `homework-reviews/2026-06-07-review.md`

批改对象：

- `daily-homework/week-03/day-04.md`
- `python-foundations/first-CLI/src/main.py`
- `learning-log/third-week-log/2026-06-07日志.md`
- `notes/third-week notes/2026-06-07笔记：logging模块.md`

学习阶段：

- [ ] 入门练习：能运行、能解释、能修明显错误
- [ ] 小步改进：开始拆函数、减少重复、补边界情况
- [x] 工程化练习：函数、测试、CLI、README

## 总评

评分：72/100
等级：勉强合格，补齐证据后可上传

一句话评价：今天的代码主功能已经完成，CLI 能记录开始、输入长度、输出路径、结束和错误分类；但学习日志是空文件，失败示例没有独立保存，也没有写给 Codex 批改的问题和明天第一步，所以作业闭环还不完整。

## 分项评分

- 当前阶段完成度：22/30
- 正确性和可运行性：23/25
- 代码可读性：12/15
- 学习日志和证据：8/20
- 明日可执行性：5/10

## 必改项

最多 1-2 条，只写当前阶段必须修的内容。

1. 补完整 `learning-log/third-week-log/2026-06-07日志.md`：写清楚今天修改了 `main.py`，记录实际运行命令、测试结果、失败示例，并用自己的话解释 logging 和 print 的区别。
2. 保存至少一份失败运行示例：缺失文件或空文件任选一个，把命令和输出写进学习日志，或单独保存到一个今日证据文档。

## 建议改进项

最多 1-3 条，分清楚“明天可以做”和“以后再做”。

1. 明天可以把 `main.py` 拆成 `main()` 函数，避免所有 CLI 逻辑都在顶层执行。
2. 明天可以给缺失文件、空文件、无效 `chunk_size` 补最小 CLI 测试，至少确认 exit code 和错误提示。
3. 以后再让 `create_json()` 接收输出路径参数，而不是固定写入当前目录的 `test.json`。

## 代码反馈

涉及文件：

- `python-foundations/first-CLI/src/main.py`
- `notes/third-week notes/2026-06-07笔记：logging模块.md`

具体问题：

- `main.py` 第 11-15 行已经正确配置 `logging.basicConfig(...)` 和 `logger = logging.getLogger(__name__)`。
- `main.py` 第 58 行记录程序开始，第 71 行记录输入长度，第 108 行记录 JSON 输出路径，第 111 行记录程序结束，符合 Day 4 的核心要求。
- `main.py` 第 63-69 行能区分文件不存在和空文件；第 73-75 行也补了无效 `chunk_size` 的错误日志，这是加分项。
- 当前 `main.py` 第 77-83 行会在所有模式下都构造 `TextStats` 和 `Wordchunk`，即使只跑 `--summary` 也会做 chunk。现在可以接受，但后续应该按参数只执行需要的功能。
- logging 笔记里 `logging.basicConfig(level = INFO)` 少了 `logging.` 或导入 `INFO`，正确写法应是 `logging.basicConfig(level=logging.INFO)`。
- logging 笔记仍有“用自己的话解释这个概念是什么”等模板残留，提交前要删掉或补成自己的内容。

验证结果：

- `./.venv/bin/python -m pytest tests -q`：`11 passed in 0.03s`
- `./.venv/bin/python src/main.py test0.txt --summary`：成功，输出 summary，exit code 0
- `./.venv/bin/python src/main.py missing.txt --summary`：失败示例，记录 `ERROR:__main__:Input file does not exist...`，exit code 1
- `./.venv/bin/python src/main.py /tmp/week3day4-empty.txt --summary`：失败示例，记录 `ERROR:__main__:Input file is empty...`，exit code 1
- `./.venv/bin/python src/main.py test0.txt --chunk_size 0 --summary`：失败示例，记录 `ERROR:__main__:Chunk size is invalid...`，exit code 1
- `./.venv/bin/python src/main.py test0.txt --createjson`：成功，记录 `JSON output created. output_path=test.json`

当前阶段不要求：

- 不要求把日志写入文件。
- 不要求引入复杂日志配置。
- 不要求接真实 LLM 或真实 Agent。
- 不要求一次性把 CLI 架构重构完。

## 学习日志反馈

- `learning-log/third-week-log/2026-06-07日志.md` 当前是空文件，不能支撑今天作业提交。
- logging 笔记能说明五种日志级别，也能解释 logging 和 print 的核心区别：print 给用户看，logging 给开发者复盘。
- 但笔记还没有把概念和今天的 `main.py` 代码绑定起来。建议补一句：本项目里 summary 输出用 `print`，而程序开始、输入长度、错误原因、输出路径用 `logging`。

## 明日修改任务

- 任务 1：补完 `learning-log/third-week-log/2026-06-07日志.md`，至少写入 4 条命令：pytest、正常 summary、缺失文件、空文件。
- 任务 2：在学习日志里整理 1-3 个给 Codex 的问题，例如“是否应该把 logger 配置放到 main.py 顶部？”、“是否应该让 create_json 接收 output_path？”。
- 任务 3：写下明天第一步：打开 `python-foundations/first-CLI/src/main.py`，把顶层逻辑移动到 `main()` 函数里。

以后再升级：

- 给 CLI 错误路径补自动化测试。
- 给 `--createjson` 增加 `--output` 参数。
- 把日志格式升级成包含时间戳，例如 `%(asctime)s %(levelname)s:%(name)s:%(message)s`。

## 是否建议上传 GitHub

- [ ] 可以上传
- [x] 修改后再上传
- [ ] 暂不上传，需要重做关键部分

说明：代码可以保留，但学习日志和失败示例还没完成。先补今日证据，再把 `main.py`、学习日志、logging 笔记和必要运行证据一起上传。

## 复查结果

复查日期：

- [ ] 通过
- [ ] 未通过

复查备注：
