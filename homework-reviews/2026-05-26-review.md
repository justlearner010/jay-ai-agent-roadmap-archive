# 2026-05-26 作业批改

日期：2026-05-26

归档文件：

- `homework-reviews/2026-05-26-review.md`

归档要求：

- 每次作业批改结果都必须保存到 `homework-reviews/`。
- 文件名使用 `YYYY-MM-DD-review.md`。
- 如果当天有复查结果，可以追加到同一个文件的“复查结果”部分。

学习阶段：

- [ ] 入门练习：能运行、能解释、能修明显错误
- [x] 小步改进：开始拆函数、减少重复、补边界情况
- [x] 工程化练习：函数、测试、CLI、README

## 总评

评分：82/100
等级：良好，但测试覆盖还不完整

一句话评价：

今天最重要的进步是开始用 pytest 测试自己的函数，并且 `word_freq` 的测试已经覆盖了基础、大小写、Top N 和空文件；主要问题是 `word_chunk` 只有一个正常输入测试，还没有覆盖边界情况。

## 分项评分

- 当前阶段完成度：24/30
- 正确性和可运行性：22/25
- 代码可读性：12/15
- 学习日志和证据：16/20
- 明日可执行性：8/10

## 必改项

最多 1-2 条，只写当前阶段必须修的内容。

1. 给 `word_chunk` 至少补 1 个边界测试，例如空文件、`chunk_size` 大于文本长度，或 `chunk_size <= 0` 的行为。Day 6 作业要求是“正常输入和边界输入”，现在 `test_word_chunk.py` 只有正常输入。
2. 清理并忽略运行测试产生的缓存文件：`python-foundations/first-CLI/__pycache__/` 和 `python-foundations/first-CLI/tests/__pycache__/` 不应该提交到 GitHub。

## 建议改进项

最多 1-3 条，分清楚“明天可以做”和“以后再做”。

1. 明天可以把 `word_chunk.py` 里的 `chunk_end` 改成 `min(i + chunk_size, len(text))`，避免最后一段的结束位置超过文本长度。
2. 明天可以在学习日志里把“测试结果：成功”改成实际命令和输出，例如 `python-foundations/first-CLI/.venv/bin/python -m pytest python-foundations/first-CLI/tests -q` 和 `5 passed`。
3. 以后再把导入路径从 `sys.path.append(...)` 升级为更标准的包结构或 `pytest.ini` 配置；当前阶段先能跑、能解释即可。

## 代码反馈

涉及文件：

- `python-foundations/first-CLI/tests/test_word_freq.py`
- `python-foundations/first-CLI/tests/test_word_chunk.py`
- `python-foundations/first-CLI/word_freq.py`
- `python-foundations/first-CLI/word_chunk.py`
- `learning-log/2026-05-26 学习日志.md`
- `notes/2026-05-26笔记：CS50P-L5-Unit test.md`

具体问题：

- 我用虚拟环境实际运行了测试：`python-foundations/first-CLI/.venv/bin/python -m pytest python-foundations/first-CLI/tests -q`，结果是 `5 passed`。
- 系统 Python 直接运行 `python3 -m pytest ...` 会失败，原因是系统环境没有安装 pytest。你需要在 README 或学习日志里写清楚使用的是项目虚拟环境。
- `test_word_freq.py` 做得比较好：用了 `tmp_path` 创建临时文件，不依赖固定样例文件；覆盖了基础词频、大小写归一、Top N、空文件。
- `test_word_chunk.py` 目前只有 `test_text_chunk_basic`，还不能证明边界情况可靠。
- `test_text_chunk_basic` 里的文件名是 `"sample].txt"`，多了一个 `]`，不影响运行，但影响阅读，建议改成 `"sample.txt"`。
- `word_freq.py` 和 `word_chunk.py` 里仍有大量旧版本注释。学习阶段可以保留一部分，但作为项目文件会显得混乱。
- `word_freq.py` 读取文件没有指定 `encoding="utf-8"`，以后处理中文文本可能不稳定。
- 当前 Git 状态里还有 `test0.txt` 未跟踪。如果它只是临时测试文件，不建议提交；如果是样例输入，就放进 README 说明用途。

当前阶段不要求：

- 不要求马上做完整 Python package。
- 不要求马上写 fixture、mock、coverage。
- 不要求今天把 CLI 所有异常都测完。
- 不要求马上实现语义分块。

## 学习日志反馈

- 日志目标明确：学习 pytest 并给项目写测试。
- 你记录了真实卡点：pytest 导入路径问题，这比只写“学了 pytest”更有价值。
- 解决方案里写了 `sys.path.append(...)`，说明你开始接触 Python 模块搜索路径，这是工程化学习的关键点。
- 需要补强的是证据：截图仍然是 Obsidian 语法 `![[...]]`，GitHub 不会正常显示；测试结果也不要只写“成功”，要写命令和输出。
- 学习笔记对 Unit Test、assert、pytest、边界情况的整理是有效的，但可以再补一小段“我今天为什么测试 empty file / top_n / chunk basic”。

## 明日修改任务

- 任务 1：给 `test_word_chunk.py` 补一个边界测试，优先测空文件或 `chunk_size` 大于文本长度。
- 任务 2：清理 `__pycache__`，并在 `.gitignore` 里确认有 `__pycache__/`、`*.pyc`、`.pytest_cache/`。

以后再升级：

- 给 `main.py` 的 CLI 行为写测试。
- 给 `chunk_size <= 0` 定义明确行为，然后写测试固定下来。
- 用 `pytest.ini` 或包结构替代测试文件里的 `sys.path.append(...)`。

## 是否建议上传 GitHub

- [ ] 可以上传
- [x] 修改后再上传
- [ ] 暂不上传，需要重做关键部分

说明：代码主方向是对的，而且当前测试已经能通过；但今天还有未跟踪的学习日志、笔记、缓存文件和本批改文件。建议先补 `word_chunk` 边界测试、清理缓存，再提交今天作业相关文件。不要提交 `__pycache__`。

## 复查结果

复查日期：

- [ ] 通过
- [ ] 未通过

复查备注：

