# 每日作业批改

日期：2026-06-18

归档文件：

- `homework-reviews/2026-06-18-review.md`

学习阶段：

- [x] 入门练习：能运行、能解释、能修明显错误
- [ ] 小步改进：开始拆函数、减少重复、补边界情况
- [ ] 工程化练习：函数、测试、CLI、README

## 总评

评分：70/100

等级：勉强合格，补完迁移和证据后再进入 Day 2

一句话评价：`SummaryMode`、`SummaryResult` 和 typed `mockSummary` 已正确写入并通过严格类型检查，但 `keywords` 没有迁移进摘要结果，错误实验、笔记和日志证据仍未闭环。

## 分项评分

- 当前阶段完成度：24/30（核心任务 20/20；迁移挑战 4/10）
- 正确性和可运行性：23/25
- 代码可读性：11/15
- 学习日志和证据：9/20
- 明日可执行性：3/10（昨日无提示回忆 2/5；明日第一步 1/5）

评分说明：

- `npm run check` 通过，`tsconfig.json` 使用严格模式，核心类型代码可被 TypeScript 正常检查。
- `npm run run:health` 通过，输出 `Week 5 TypeScript: environment ready = true`。
- `keywords` 被单独建模为 `KEY`，没有加入 `SummaryResult` 和 `mockSummary`，因此没有完成题目要求的“扩展摘要结果”迁移。
- 日志和随任务笔记仍缺实验现象、故意错误、迁移结果和完成后的理解修正。

## 必改项

1. 在 `SummaryResult` 中增加 `keywords: string[]`，并在 `mockSummary` 中提供字符串数组；删除不再需要的独立 `KEY`/`keywords` 包装结构。
2. 完成一次失败和一次成功检查：先在 `keywords` 中混入数字并记录 TypeScript 关键报错，再修复并记录 `npm run check` 通过；同步补入笔记和学习日志。

## 建议改进项

1. 明天可以做：把“明天第一步”改成具体动作，例如“不看笔记重写 `SummaryMode` 和 `SummaryResult`，再开始 Day 2 request interface”。
2. 明天可以做：统一提示记录。随任务笔记写“使用提示后完成”，但日志和提示等级选择 0，两者应按实际情况填写。
3. 以后再做：统一 TypeScript 格式和命名，例如类型名使用清楚的业务名称，冒号后保留空格；当前不需要引入格式化工具。

## 代码反馈

涉及文件：

- `week-05/type-practice.ts`
- `week-05/environment-check.ts`
- `week-05/tsconfig.json`
- `notes/fifth-week notes/2026-06-18笔记；Typescript基本语法.md`

具体问题：

- `SummaryMode` 的 union 正确限制了 `brief` 和 `bullets`。
- `SummaryResult` 的三个核心字段类型正确，`mockSummary` 也符合该类型。
- `ReadingProgress` 跟做实验已经落盘，但没有记录把数字改成字符串时的报错现象。
- 迁移挑战应扩展 `SummaryResult`，而不是另外创建与摘要结果无关的 `{ strs: string[] }` 包装对象。
- `KEY` 和 `strs` 命名无法表达“摘要关键词”，建议直接使用 `keywords` 字段。

当前阶段不要求：

- 不要求学习泛型、复杂 interface 差异或 Next.js 页面。
- 不要求自动化测试；今天只需要 TypeScript 失败/成功检查证据。

## 学习日志反馈

- 初始预测与修正：保留“不知道 JavaScript 是否立即报错”是有效的初始记录。
- 当前概念解释：已经意识到 `number` 不接收字符串。更准确的说法是：JavaScript 有运行时值类型，但不会按 TypeScript 的静态数据契约检查对象；TypeScript 在开发或构建检查阶段提示不匹配。
- 迁移挑战：代码中出现了字符串数组，但日志未填写结果，而且实现没有扩展摘要类型。
- 最高提示等级：日志选择 0，随任务笔记却勾选“使用提示后完成”，需要统一。
- Commit：日志填写了 `add Week-05 practice`，但当前 `week-05/` 仍是未跟踪内容，最近提交中也没有该提交；应填写“未提交”或真实 commit hash。

## 明日修改任务

- 10 分钟无提示回忆：不看笔记写出 `SummaryMode` 和包含 `keywords` 的 `SummaryResult`，并解释 union 比普通 `string` 多限制了什么。
- 任务 1：把 `keywords` 正确整合进 `SummaryResult` 和 `mockSummary`。
- 任务 2：记录一次失败检查和一次修复后通过检查，并补全日志中的迁移结果、提示等级和明日第一步。

以后再升级：

- 完成本次必改项并复查通过后，再进入 Day 2 的 request/response interface 和运行时校验。

## 是否建议上传 GitHub

- [x] 可以上传
- [ ] 修改后再上传
- [ ] 暂不上传，需要重做关键部分

## 复查结果

复查日期：2026-06-18

- [x] 通过
- [ ] 未通过

复查备注：

- 第一次 43 分的批改基于未保存的空文件，该评分已作废。
- 保存后重新读取代码并运行检查，当前有效评分为 70/100。
- 2026-06-18 再次复查：`SummaryResult` 和 `mockSummary` 已加入字符串数组，独立的 `KEY` 包装已删除。
- 日志已记录故意混入错误类型时的报错和修复结果；复查运行 `npm run check` 与 `npm run run:health` 均通过。
- 当前只剩一个必改项：任务和后续 API 契约使用复数 `keywords`，代码目前使用单数 `keyword`。字段名不同会被视为两个不同契约，统一改名后即可通过本次复查。
- 2026-06-21 里程碑复查：字段已统一为复数 `keywords`；`npm run check` 和 `npm run run:health` 均通过。必改项关闭。
