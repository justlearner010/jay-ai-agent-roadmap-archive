# 每日作业批改

日期：2026-06-06

归档文件：

- `homework-reviews/2026-06-06-review.md`

批改对象：

- `daily-homework/week-03/day-03.md`
- `learning-log/third-week-log/2026-06-06日志：.md`
- `notes/third-week notes/2026-06-06笔记：Mock-LLM.md`
- `python-foundations/first-CLI/src/main.py`
- `python-foundations/first-CLI/src/summarize.py`

学习阶段：

- [ ] 入门练习：能运行、能解释、能修明显错误
- [ ] 小步改进：开始拆函数、减少重复、补边界情况
- [x] 工程化练习：函数、测试、CLI、README

## 总评

评分：63/100
等级：勉强合格，明天必须先修改

一句话评价：今天完成了 mock LLM 的第一版函数和 CLI 参数扩展，方向是对的；但 `summarize.py` 导入时会直接抛 `ValueError`，导致 `main.py` 的所有模式都无法启动，所以今天还没有形成可运行闭环。

## 分项评分

- 当前阶段完成度：18/30
- 正确性和可运行性：9/25
- 代码可读性：10/15
- 学习日志和证据：17/20
- 明日可执行性：9/10

## 必改项

最多 1-2 条，只写当前阶段必须修的内容。

1. 修复 `python-foundations/first-CLI/src/summarize.py` 第 14 行的缩进：`raise ValueError(...)` 必须放进 `summarize_text()` 函数内部，并且只能在 `mode` 不是 `brief` 或 `bullets` 时执行。现在它在文件顶层，导致任何 `import summarize` 都会立刻失败。
2. 修复 `bullets` 分支里的 `len(text.split)`：这里少了 `()`，应该调用 `text.split()`，否则以后运行 bullets mode 会得到函数对象而不是单词列表。

## 建议改进项

最多 1-3 条，分清楚“明天可以做”和“以后再做”。

1. 明天可以补一个 `--summary_mode` 参数，取值只允许 `brief` 和 `bullets`，这样 CLI 才真正支持两种 mock 总结模式。
2. 明天可以给 `summarize_text()` 单独写 3 个测试：`brief`、`bullets`、非法 mode。
3. 以后再把 `main.py` 改成 `main()` 函数结构，避免顶层逻辑越来越长，也方便测试 CLI 参数。

## 代码反馈

涉及文件：

- `python-foundations/first-CLI/src/summarize.py`
- `python-foundations/first-CLI/src/main.py`

具体问题：

- 实际运行 `python-foundations/first-CLI/.venv/bin/python -m pytest python-foundations/first-CLI -q`，结果是 `8 passed in 0.12s`。这说明旧的 `word_chunk`、`text_status`、`word_freq` 测试仍然通过。
- 现有测试没有覆盖 `summarize.py` 和 `main.py` 入口，所以测试通过不能证明今天的 mock LLM 功能可用。
- 实际运行 `python3 src/main.py test1.txt --summary` 失败，错误是 `ValueError: mode must be 'brief' or 'bullets'`，错误发生在 `from summarize import summarize_text` 导入阶段。
- 实际运行 `python3 src/main.py test1.txt --freq` 和 `python3 src/main.py test1.txt --status` 也同样失败。也就是说，新增的 summarize 模块现在影响了整个 CLI，不只是 summary 功能。
- `main.py` 已经把 `--summary`、`--freq`、`--status`、`--createjson` 拆成可选开关，这个方向比之前全部默认执行更清楚。
- 当前 `--summary` 只固定调用 `mode="brief"`，还没有从命令行支持 `bullets`。

当前阶段不要求：

- 不要求接真实 LLM API。
- 不要求 summary 真的理解文章内容。
- 不要求把 CLI 打包成可安装命令。
- 不要求一次性补完整端到端测试。

## 学习日志反馈

- 日志写清楚了今天完成：新增可选参数、写 `summarize_text(text, mode)`、用 mock 规则返回固定格式总结。
- 卡点写得真实，尤其是“不知道 mock 的用途”和“不知道为什么调用 summarize 函数不成功”，这正好对应今天的学习目标。
- 证据部分记录了 `--freq` 和 `--status` 的成功输出，但当前我实际运行时两者已经会因为 `summarize.py` 导入失败而失败。建议明天修完后重新跑一遍，把最新输出覆盖到日志里。
- Mock LLM 笔记能解释为什么不用真实 API，内容方向合格；但它更像概念笔记，还可以补一句“本项目里 mock LLM 的接口就是 `summarize_text(text, mode)`”。

## 明日修改任务

- 任务 1：修复 `python-foundations/first-CLI/src/summarize.py` 的 `raise ValueError` 缩进和 `text.split()` 调用。
- 任务 2：运行并记录三条命令：`--summary`、`--freq`、`--status`，确认新增 summarize 模块没有破坏其它模式。
- 任务 3：给 `summarize_text()` 新增最小测试，至少覆盖 `brief` 和 `bullets`。

以后再升级：

- 增加 `--summary_mode brief|bullets`。
- 把 `summarize_text()` 的返回格式统一成字符串，或者在 `main.py` 中专门处理 list 输出。
- 给非法 mode、空文本、短文本补边界测试。

## 是否建议上传 GitHub

- [ ] 可以上传
- [x] 修改后再上传
- [ ] 暂不上传，需要重做关键部分

说明：今天的思路和文件值得保留，但目前 CLI 主入口无法启动。先修 `summarize.py`，确认 `--summary`、`--freq`、`--status` 都能跑，再上传。

## 复查结果

复查日期：

- [ ] 通过
- [ ] 未通过

复查备注：

## 复查结果：2026-06-06 第二次批改

复查对象：

- `python-foundations/first-CLI/src/summarize.py`
- `python-foundations/first-CLI/tests/test_summarize.py`
- `python-foundations/first-CLI/src/main.py`

复查评分：83/100
等级：合格，有少量修改项

一句话评价：两个必改项已经完成，mock LLM 函数现在可以正常导入，`brief` 和 `bullets` 都有测试覆盖；CLI 的 `--summary`、`--freq`、`--status` 也都能打印预期输出，但成功命令仍返回 exit code 1，上传前建议再修一次。

复查通过情况：

- [x] `raise ValueError(...)` 已经移入 `summarize_text()` 函数内部。
- [x] `len(text.split)` 已经改成 `len(text.split())`。
- [x] 新增 `tests/test_summarize.py`，覆盖 `brief`、`bullets` 和非法 mode。
- [x] `python-foundations/first-CLI/.venv/bin/python -m pytest python-foundations/first-CLI -q` 通过：`11 passed in 0.02s`。
- [x] `python3 src/main.py test1.txt --summary` 能输出：`Brief summary: this text has 165 words`。
- [x] `python3 src/main.py test1.txt --freq` 能输出词频 Top 10。
- [x] `python3 src/main.py test1.txt --status` 能输出行数、单词数、数字数、空格数。

仍需修改：

1. `main.py` 最后一行 `sys.exit("运行完毕")` 会让成功命令返回 exit code 1。命令行工具成功运行时应该返回 0，建议改成 `print("运行完毕")` 或 `sys.exit(0)`。
2. CLI 目前只固定调用 `summarize_text(text, mode="brief")`，还没有通过命令行选择 `bullets`。这不是今天必须项，但如果要完整对齐 Day 3，可以后续加 `--summary_mode brief|bullets`。

是否建议上传 GitHub：

- [ ] 可以上传
- [x] 修改后再上传
- [ ] 暂不上传，需要重做关键部分

说明：今天的核心 bug 已修，分数从 63 提升到 83。建议再修掉成功路径 exit code 1，再和测试一起上传。
