# 每日作业批改

日期：2026-06-10

归档文件：

- `homework-reviews/2026-06-10-review.md`

批改对象：

- `daily-homework/week-03/day-07.md`
- `learning-log/weekly_review/第三周复盘.md`
- `trackers/weekly-log.csv`
- `trackers/homework-review-log.csv`
- `python-foundations/first-CLI`

学习阶段：

- [ ] 入门练习：能运行、能解释、能修明显错误
- [ ] 小步改进：开始拆函数、减少重复、补边界情况
- [x] 工程化练习：函数、测试、CLI、README

## 总评

评分：78/100
等级：合格，但周复盘还需要补关键证据

一句话评价：CLI 骨架本身已经能跑，测试也达到 `15 passed`，第三周复盘覆盖了时间投入、进步、困难和下周计划；但 Day7 明确要求“列出 CLI 当前支持和不支持的功能”，这部分还不够清楚，证据链接和追踪表也偏泛或过期。

## 分项评分

- 当前阶段完成度：22/30
- 正确性和可运行性：24/25
- 代码可读性：12/15
- 学习日志和证据：14/20
- 明日可执行性：6/10

## 必改项

最多 1-2 条，只写当前阶段必须修的内容。

1. 在 `learning-log/weekly_review/第三周复盘.md` 里补一个“CLI 当前支持 / 暂不支持”小节，至少列出：支持 `--summary`、`--status`、`--freq`、`--createjson`；暂不支持 `summary_mode`、自定义输出路径、完整 CLI subprocess 测试。
2. 更新 `trackers/weekly-log.csv` 里第三周证据，把旧的 `first-CLI 11 passed` 改成当前真实结果 `first-CLI 15 passed`，并补上周复盘文件路径。

## 建议改进项

最多 1-3 条，分清楚“明天可以做”和“以后再做”。

1. 明天可以把周复盘里的“然后就是这个”删掉或补完整，否则会显得文档没有收尾。
2. 明天可以把证据链接从 `third-week-log`、`third-week notes` 改成具体文件路径，例如 `learning-log/third-week-log/2026-06-09日志.md`。
3. 以后再补一个 `tests/test_cli.py`，用 subprocess 验证 `python src/main.py test0.txt --summary` 这类真实命令。

## 代码反馈

涉及文件：

- `python-foundations/first-CLI/src/main.py`
- `python-foundations/first-CLI/src/create_json.py`
- `python-foundations/first-CLI/tests/`

具体问题：

- `./.venv/bin/python -m pytest tests -q` 通过：`15 passed in 0.09s`。
- `--summary` 能输出 mock summary，并记录开始、输入长度、结束日志。
- `--status` 能输出行数、单词数、数字数、空格数。
- `--freq` 能输出词频列表。
- `--createjson` 能生成 `output/test0.json`，且 JSON 包含 `summary`、`mode`、`input_chars`。
- 当前 CLI 骨架已经具备可演示性，但 `main.py` 仍会在所有模式下先计算 `chunks`、`TextStats` 等数据，后续可以按参数懒执行。

验证结果：

- `./.venv/bin/python -m pytest tests -q`：`15 passed in 0.09s`
- `./.venv/bin/python src/main.py test0.txt --summary`：成功
- `./.venv/bin/python src/main.py test0.txt --status`：成功
- `./.venv/bin/python src/main.py test0.txt --freq`：成功
- `rm -rf output && ./.venv/bin/python src/main.py test0.txt --createjson && ./.venv/bin/python -m json.tool output/test0.json`：成功

当前阶段不要求：

- 不要求接真实 LLM。
- 不要求把 CLI 发布成 pip 包。
- 不要求完整重构 `main.py`。
- 不要求今天补完所有 subprocess 测试。

## 学习日志反馈

- 第三周复盘能看出你对 Python 工程化的理解在加深，尤其是 logging、tmp_path、pathlib、JSON 输出这些点已经从“知识点”进入项目上下文。
- 时间投入记录比较完整，能反映本周学习结构。
- “本周嵌入的科班基础”写成了“无”，这个判断偏保守。本周其实嵌入了文件系统、异常处理、命令行参数、测试隔离、结构化数据这些基础。
- “证据链接”过于笼统，后续批改时不容易复查。
- 没有明确写 1-3 个提交给 Codex 的具体问题，这一点和 Day7 作业要求不完全对齐。

## 明日修改任务

- 任务 1：在 `learning-log/weekly_review/第三周复盘.md` 增加“CLI 当前支持 / 暂不支持”小节。
- 任务 2：更新 `trackers/weekly-log.csv` 的第三周证据，写入 `first-CLI 15 passed` 和 `learning-log/weekly_review/第三周复盘.md`。

以后再升级：

- 给 `main.py` 写 subprocess 测试。
- 把 `main.py` 中各模式的数据计算改成按需执行。
- 把 README 更新成一个可运行演示文档。

## 是否建议上传 GitHub

- [ ] 可以上传
- [x] 修改后再上传
- [ ] 暂不上传，需要重做关键部分

说明：代码和测试已经可以上传，但 Day7 是周复盘任务。先补 CLI 支持/不支持清单和 weekly-log 证据，再上传会更完整。

## 复查结果

复查日期：

- [ ] 通过
- [ ] 未通过

复查备注：
