# 每日作业批改

日期：2026-06-15

归档文件：

- `homework-reviews/2026-06-15-review.md`

对应任务：

- `daily-homework/week-04/day-05.md`
- 任务主题：真实 LLM 接口设计说明 / OpenAI API 接入边界

学习阶段：

- [ ] 入门练习：能运行、能解释、能修明显错误
- [x] 小步改进：开始拆函数、减少重复、补边界情况
- [ ] 工程化练习：函数、测试、CLI、README

## 总评

评分：52/100
等级：需要重做关键部分

一句话评价：

你已经开始理解“真实 LLM 接口”和 mock LLM 的区别，也尝试把 OpenAI 接入 `summarize.py`，但当前实现把外部 SDK 依赖直接放进了导入链，导致本地测试在收集阶段失败；这说明接口边界还没有设计稳。

## 分项评分

- 当前阶段完成度：15/30
- 正确性和可运行性：3/25
- 代码可读性：10/15
- 学习日志和证据：16/20
- 明日可执行性：8/10

## 必改项

最多 1-2 条，只写当前阶段必须修的内容。

1. `summarize.py` 不能在模块顶层强依赖 `openai`。现在 `from openai import OpenAI` 会让 `pytest` 在收集 `tests/test_summarize.py` 时直接失败，因为项目环境里没有安装 `openai`。
2. 统一 LLM 接口。`Summarizer.run()` 调用的是 `self.llm.summarize(text)`，但 `OpenAILLM` 暴露的是 `summarize_with_llm()`，这会导致真实 LLM 类接进 `Summarizer` 时接口不匹配。

## 建议改进项

最多 1-3 条，分清楚“明天可以做”和“以后再做”。

1. 明天可以做：把 `OpenAILLM` 改成可注入 client 的结构，测试里传 fake client，不直接 patch OpenAI SDK 内部路径。
2. 明天可以做：`test_summarize.py` 删除 `from openai import OpenAI`，本地单元测试只测你自己的接口契约，不依赖真实 SDK。
3. 以后再做：等 mock、错误处理、空文本、接口说明都稳定后，再单独加真实 OpenAI 集成测试，并用环境变量控制是否运行。

## 代码反馈

涉及文件：

- `python-foundations/first-CLI/src/summarize.py`
- `python-foundations/first-CLI/tests/test_summarize.py`
- `notes/fourth-week notes/2026-06-15笔记：OpenAI API.md`
- `learning-log/fourth-week-log/2026-06-15 日志.md`

具体问题：

- `summarize.py` 顶层导入 `openai`，但 `pyproject.toml` 和 `requirements.txt` 都没有声明 `openai` 依赖，导致测试无法开始。
- `OpenAILLM.summarize_with_llm()` 和 `Summarizer.run()` 期待的 `.summarize()` 方法不一致。
- `OpenAILLM.__init__()` 里直接创建 `OpenAI()`，会把 API key、网络环境、SDK 初始化都提前绑定到对象创建阶段，不利于测试。
- `test_summarize.py` 也直接 `from openai import OpenAI`，这不是必要依赖，会让本地基础测试变成外部 SDK 测试。
- monkeypatch 目标 `"openai.resources.responses.Responses.create"` 太贴近 SDK 内部实现，当前阶段应该优先 patch 自己定义的 fake client 或 fake LLM。

当前阶段不要求：

- 不要求真的调用 OpenAI API。
- 不要求把 API key 写进代码或日志。
- 不要求测试真实网络请求。
- 不要求覆盖所有 OpenAI 参数。

## 学习日志反馈

- 日志里记录了真实失败：`Interrupted: 1 error during collection`，这是好的，说明你没有假装测试通过。
- 但是“明天第一步：修改今日错题”还不够具体。明天第一步应该写成：先移除测试收集阶段对 `openai` 包的硬依赖，再统一 `summarize()` 接口。
- OpenAI API 笔记已经覆盖了环境变量、SDK、Responses API 等概念，但还留有模板句子，需要删掉占位内容，改成你自己的理解。

## 明日修改任务

- 任务 1：修改 `summarize.py`，让所有 LLM provider 都实现同一个方法名：`summarize(text)`。
- 任务 2：修改 `test_summarize.py`，删除 `from openai import OpenAI`，用 fake client / fake LLM 测试 `Summarizer`，保证 `pytest` 至少能正常收集并运行。

以后再升级：

- 给 `OpenAILLM` 增加错误处理：API 失败、超时、空返回时返回清晰错误。
- 在文档中补充接口说明：输入、输出、错误、超时、API key 为什么不能暴露在前端。
- 如果要保留真实 OpenAI 测试，放到单独的 integration test，并用环境变量开关。

## 验证记录

执行目录：

- `python-foundations/first-CLI`

执行命令：

```bash
./.venv/bin/python -m pytest
```

结果：

```text
collected 17 items / 1 error
ERROR collecting tests/test_summarize.py
ModuleNotFoundError: No module named 'openai'
Interrupted: 1 error during collection
```

结论：

- 当前作业未通过。
- 失败不是算法逻辑问题，而是接口边界和依赖管理问题。
- 修复优先级：先让测试不依赖真实 OpenAI SDK，再讨论真实 API 接入。

## 是否建议上传 GitHub

- [ ] 可以上传
- [ ] 修改后再上传
- [x] 暂不上传，需要重做关键部分

## 复查结果

复查日期：2026-06-15

- [x] 通过
- [ ] 未通过

复查备注：

- 已修复 `openai` 顶层导入导致的测试收集失败。
- 已统一 LLM 接口为 `summarize(text)`。
- 已将真实 OpenAI SDK 调用放到 `OpenAILLM._get_client()` 内延迟导入，并允许测试注入 fake client。
- 复查命令 1：`./.venv/bin/python -m pytest tests/test_summarize.py`
- 复查结果 1：`13 passed, 1 xfailed`
- 复查命令 2：`./.venv/bin/python -m pytest`
- 复查结果 2：`30 passed, 1 xfailed`
