# 每日作业批改

日期：2026-06-19

归档文件：

- `homework-reviews/2026-06-19-review.md`

学习阶段：

- [x] 入门练习：能运行、能解释、能修明显错误
- [ ] 小步改进：开始拆函数、减少重复、补边界情况
- [ ] 工程化练习：函数、测试、CLI、README

## 总评

评分：78/100

等级：合格，修正核心概念后进入 Day 3

一句话评价：request/response interface、运行时校验和三组 mock 已经形成可运行闭环，但目前仍把 `as` 误认为数据检查，并把校验方法和值字段错误地写进了 `UserProfile` 数据结构。

## 分项评分

- 当前阶段完成度：30/30（核心任务 20/20；迁移挑战 10/10）
- 正确性和可运行性：22/25
- 代码可读性：11/15
- 学习日志和证据：11/20
- 明日可执行性：4/10（昨日无提示回忆 2/5；明日第一步 2/5）

评分说明：

- `SummarizeRequest` 和 `SummarizeResponse` 字段完整，复用了正确的 `SummaryMode`。
- `validateMockResponse` 能拒绝 `null`、非对象、缺少 `inputChars`、错误字段类型和非法 mode。
- `npm run check` 通过；运行 `type-practice.ts` 得到 `true`、`false`、`false`，与三组 mock 预期一致。
- 日志记录了运行命令、结果、迁移解释和提示等级，但对 `as` 的解释仍然错误，明日第一步也不够具体。

## 必改项

1. 修正学习日志中的第二个预测：`as SummarizeResponse` 不检查、不补全数据，只是告诉编译器暂时按这个类型看待 value；真正的校验是后续 `typeof`、字段存在性和 mode 条件。
2. 把 `UserProfile` interface 恢复为纯数据形状，只保留 `name` 和 `level`。删除其中的 `validateUserProfile` 和 `value` 字段，校验函数继续放在 interface 外部。

## 建议改进项

1. 明天可以做：删除空的 `week-05/type-practice2.ts`，避免误以为 Day 2 代码在该文件中。
2. 明天可以做：把“明天第一步”改为“画出标题、输入框、提交按钮、结果区域，并标出结果区域读取 `SummarizeResponse`”。
3. 以后再做：把校验函数进一步写成 type predicate；当前阶段先把 boolean 校验和类型断言边界解释清楚。

## 代码反馈

涉及文件：

- `week-05/type-practice.ts`
- `week-05/type-practice2.ts`
- `learning-log/fifth-week log/2026-06-19日志.md`
- `notes/fifth-week notes/2026-06-19笔记：interface.md`

具体问题：

- `SummarizeRequest`、`SummarizeResponse` 和 `validateMockResponse` 的主流程正确。
- 三份 mock 分别覆盖有效响应、缺少 `inputChars` 和非法 `mode`，满足迁移要求。
- `UserProfile` 本应描述外部对象的数据形状，却额外要求 `validateUserProfile` 方法和 `value` 字段；这与实际待校验对象不一致。
- `const obj = value as UserProfile` 没有让 value 自动拥有这些字段，也没有执行验证。函数之所以有效，是因为后续显式检查了 `name` 和 `level`。
- `type-practice2.ts` 当前为空；代码实际写入了 `type-practice.ts`。

当前阶段不要求：

- 不要求引入 Zod 等校验库。
- 不要求实现泛型、interface 继承或复杂 type predicate。
- 不要求提前创建 Next.js API route。

## 学习日志反馈

- 初始预测与修正：第一项“不会自动补字段”正确；第二项“as 是检查数据”错误，必须在复查前修正。
- 当前概念解释：应区分“interface 描述预期形状”和“validator 检查真实值”。现在两句话仍把两个阶段混在一起。
- 迁移挑战：缺字段和非法 mode 的原因解释正确。
- 最高提示等级：记录为 3，与使用半成品完成的实际情况一致；下次先尝试自己列校验顺序，再展开提示 3。

## 明日修改任务

- 10 分钟无提示回忆：不看代码回答“interface、as、validator 各自做什么”，并各写一句。
- 任务 1：清理 `UserProfile` interface，修正日志中的 `as` 解释。
- 任务 2：画出 Day 3 页面四个区域，并标出 typed mock 在哪个区域被读取。

以后再升级：

- 页面跑通后，再把 boolean validator 升级为更完整的类型收窄工具。

## 是否建议上传 GitHub

- [x] 可以上传
- [ ] 修改后再上传
- [ ] 暂不上传，需要重做关键部分

## 复查结果

复查日期：2026-06-21

- [x] 通过
- [ ] 未通过

复查备注：

- `UserProfile` 已恢复为纯数据形状，只保留 `name` 和 `level`；校验函数保留在 interface 外部。
- 学习日志已明确区分 `interface`、`as` 和 validator：`as` 不会检查或补全数据，运行时校验由显式条件完成。
- 复查命令 `npm run check && npx tsx type-practice.ts` 退出码为 0，输出 `true`、`false`、`false`，与三组 mock 预期一致。本次必改项关闭。
