# 2026-05-28 作业批改

日期：2026-05-28

归档文件：

- `homework-reviews/2026-05-28-review.md`

归档要求：

- 每次作业批改结果都必须保存到 `homework-reviews/`。
- 文件名使用 `YYYY-MM-DD-review.md`。
- 如果当天有复查结果，可以追加到同一个文件的“复查结果”部分。

学习阶段：

- [ ] 入门练习：能运行、能解释、能修明显错误
- [x] 小步改进：开始拆函数、减少重复、补边界情况
- [x] 工程化练习：函数、测试、CLI、README

## 总评

评分：74/100
等级：勉强合格，明天必须先修测试和项目结构

一句话评价：

今天你完成了 `str_reverse` 和 `list_max` 两个函数练习，并开始使用 `pytest.ini` 管理导入路径；但测试只覆盖正常输入，而且 `first-CLI` 迁移到 `src/` 后原测试已经失败，所以当前工程状态还不够稳。

## 分项评分

- 当前阶段完成度：22/30
- 正确性和可运行性：17/25
- 代码可读性：11/15
- 学习日志和证据：15/20
- 明日可执行性：9/10

## 必改项

最多 1-2 条，只写当前阶段必须修的内容。

1. 修复 `first-CLI` 的测试导入路径。现在 `python-foundations/first-CLI` 代码已经移动到 `src/`，但测试仍然 `from word_chunk import text_chunk`、`from word_freq import word_freq_cnt`，运行 pytest 会报 `ModuleNotFoundError`。
2. 给今天的 `practice` 两个函数补边界测试：`str_reverse("")`、`str_reverse("a")`、`list_max([-1, -3])`、`list_max([])` 至少选 2 个。现在每个文件只有一个正常输入测试，测试价值偏弱。

## 建议改进项

最多 1-3 条，分清楚“明天可以做”和“以后再做”。

1. 明天可以把 `_init_.py` 改成标准文件名 `__init__.py`。现在的 `_init_.py` 只是普通文件，不能起到 Python package 初始化文件的作用。
2. 明天可以把函数参数名从 `str`、`list` 改成 `text`、`numbers`，避免覆盖 Python 内置类型名。
3. 以后再处理更完整的 package 结构；当前阶段先把 `pytest.ini + src + tests` 这套最小结构跑通。

## 代码反馈

涉及文件：

- `python-foundations/practice/src/str_reverse.py`
- `python-foundations/practice/src/list_max.py`
- `python-foundations/practice/tests/test_str_reverse.py`
- `python-foundations/practice/tests/test_list_max.py`
- `python-foundations/practice/pytest.ini`
- `python-foundations/first-CLI/pytest.ini`
- `python-foundations/first-CLI/tests/test_word_chunk.py`
- `python-foundations/first-CLI/tests/test_word_freq.py`

具体问题：

- 我运行 `python-foundations/practice/.venv/bin/python -m pytest python-foundations/practice/tests -q`，结果是 `2 passed`，说明今天新增的 practice 测试可以通过。
- 我运行 `python-foundations/first-CLI/.venv/bin/python -m pytest python-foundations/first-CLI/tests -q`，结果失败，两个测试文件都报 `ModuleNotFoundError`。
- `practice/pytest.ini` 里 `pythonpath = src` 的方向是对的，可以让测试直接导入 `str_reverse` 和 `list_max`。
- `str_reverse()` 当前会 `print(rstr)`，这对函数测试不是必须的。更好的方式是函数只 `return`，需要展示时由调用方 `print`。
- `list_max([])` 现在会直接抛出 `ValueError`。可以接受，但需要在测试里明确这个行为；否则以后你不知道空列表应该返回什么。
- `first-CLI` 里旧的顶层 `create_json.py`、`main.py`、`word_chunk.py`、`word_freq.py` 当前显示为删除。如果这是迁移到 `src/` 的整理动作，需要和测试导入路径一起提交；如果不是故意删除，要先恢复。
- 当前还有 `__pycache__` 未跟踪文件，不应该提交。

当前阶段不要求：

- 不要求一次做成可发布 Python 包。
- 不要求写复杂 fixture。
- 不要求加入 coverage。
- 不要求今天完成所有 Python 基础练习。

## 学习日志反馈

- 今天日志目标明确：补 Python 函数基础，并给两个小函数加 pytest。
- 你记录了真实卡点：不知道怎么配置 pytest 读取入口。
- `pytest.ini` 笔记方向正确，但需要修正一个关键概念：文件名应该是 `__init__.py`，不是 `_init_.py`。
- “测试结果：passed”还是太简略。建议以后写成实际命令和输出，例如：

```bash
python-foundations/practice/.venv/bin/python -m pytest python-foundations/practice/tests -q
# 2 passed
```

- 今天的学习日志没有写 commit hash。虽然 Git 里能看到 `6d7b21c Add practice`，但日志里最好写清楚。

## 明日修改任务

- 任务 1：修 `first-CLI` 测试导入路径，让 `first-CLI` 的 pytest 从项目目录和仓库根目录都能运行。
- 任务 2：给 `practice` 的两个函数补边界测试，并把 `_init_.py` 改成 `__init__.py`。

以后再升级：

- 给 `list_max([])` 定义明确行为：抛错、返回 `None`，或自定义错误信息。
- 把 `str_reverse()` 改成无副作用函数，只返回结果。
- 给每个小练习补 README 或运行说明。

## 是否建议上传 GitHub

- [ ] 可以上传
- [x] 修改后再上传
- [ ] 暂不上传，需要重做关键部分

说明：今天的 `practice` 小练习可以作为作业基础，但当前 `first-CLI` 测试失败，且工作区还有删除旧文件、缓存文件、学习日志和笔记未提交。建议先修测试和边界用例，再分批提交；不要提交 `__pycache__`、`.pytest_cache` 或 `.venv`。

## 复查结果

复查日期：

- [ ] 通过
- [ ] 未通过

复查备注：

