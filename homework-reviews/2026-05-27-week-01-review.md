# 第一周作业批改

日期：2026-05-27

批改对象：

- `learning-log/weekly_review/第一周复盘.md`
- `learning-log/first-week-log/`
- `homework-reviews/2026-05-21-revised-review.md` 到 `homework-reviews/2026-05-26-review.md`
- `python-foundations/first-CLI/`

## 总评

评分：83/100
等级：第一周合格，项目方向正确，但复盘和证据链需要补齐

一句话评价：

这一周不是零散学语法，而是完成了从文件读取、文本统计、词频、chunk、JSON、CLI 到 pytest 的连续小项目链路；主要短板是周复盘写得太粗，证据链接、原计划、下周计划和 Git 状态没有整理清楚。

## 分项评分

- 本周目标完成度：25/30
- 代码与测试质量：21/25
- 学习复盘质量：14/20
- GitHub 证据链：14/15
- 下周计划可执行性：9/10

## 本周完成情况

做得好的地方：

- 完成了文本处理主线：文件读取、行数/单词数/数字统计、词频 Top N、文本 chunk、JSON 输出。
- 把单文件练习推进成了 `first-CLI` 项目，开始拆分 `main.py`、`word_freq.py`、`word_chunk.py`、`text_status.py`、`create_json.py`。
- 开始使用 `argparse`，这是从“练习脚本”走向“命令行工具”的关键一步。
- 开始写 pytest，并且 `word_freq` 测试覆盖了基础场景、大小写、Top N、空文件。
- GitHub 上已经有连续提交，说明本周不是只在本地练习。

没有完全完成的地方：

- `this-week.md` 里要求“至少两个练习有测试”，目前严格看只有 `word_freq` 覆盖较完整；`word_chunk` 只有一个正常输入测试，还缺边界测试。
- `weekly-log.csv` 里的本周记录还没有填写实际投入、成果链接和复盘备注。
- 周复盘里“原计划交付物”写了“未知”，但仓库里其实有 `this-week.md` 和 `weekly-log.csv` 可以对照。
- 周复盘缺少“下周计划”部分，这是模板里的关键字段。

## 周复盘文件反馈

涉及文件：

- `learning-log/weekly_review/第一周复盘.md`

具体问题：

- “实际交付物：first-CLI”太窄。本周实际交付物应该写成：`Python 文本处理练习链路 + first-CLI + pytest 初步测试 + 每日学习日志/批改记录`。
- “证据链接”太笼统。建议写具体路径或 commit，例如 `python-foundations/first-CLI/`、`homework-reviews/2026-05-26-review.md`、`d6f4235 add two pytest of word_chunk and word_freq`。
- “本周困难”写得真实，但还缺“怎么解决 / 下周怎么避免”。例如 GitHub commit 问题，应该沉淀成规则：每次只提交一类文件，不再 `git add .`。
- “下周计划”缺失。周复盘不是只总结过去，还要收敛下周动作。
- 时间投入合计较大，但没有证据拆分。当前可以保留估算，但下周最好每天日志里同步记录，避免周末回忆。

## 代码与测试反馈

涉及文件：

- `python-foundations/first-CLI/`
- `python-foundations/first-CLI/tests/test_word_freq.py`
- `python-foundations/first-CLI/tests/test_word_chunk.py`

我实际运行：

```bash
python-foundations/first-CLI/.venv/bin/python -m pytest python-foundations/first-CLI/tests -q
```

结果：

```text
5 passed in 0.02s
```

代码层面的主要问题：

- `test_word_freq.py` 是本周测试质量最高的文件，已经有多个场景。
- `test_word_chunk.py` 还缺边界测试，建议补：空文件、`chunk_size` 大于文本长度、最后一段不足 chunk size。
- `word_chunk.py` 的 `chunk_end` 仍建议改成 `min(i + chunk_size, len(text))`。
- 项目里仍有临时文件和缓存风险：`__pycache__`、`test0.txt` 需要判断是否保留。
- 旧代码大段注释仍然较多，下周应逐步把旧版本移动到学习日志，项目文件保留当前可运行版本。

## Git 和证据链反馈

当前 Git 状态显示：学习日志被移动到了 `learning-log/first-week-log/`，原位置显示删除，新位置显示未跟踪。这可能是正常归档，但提交时要把它当作一次“整理第一周学习日志”的独立提交。

建议分三批处理：

1. 第一批：周复盘和本周批改文件。
2. 第二批：学习日志移动到 `learning-log/first-week-log/` 的归档提交。
3. 第三批：代码清理和测试补充。

不要把这些和 `resources/learning-resources.md`、旧 Python 文件删除、`scripts/` 一起混提交。

## 必改项

1. 补全 `learning-log/weekly_review/第一周复盘.md`：把“原计划交付物”“证据链接”“下周计划”写具体。
2. 给 `test_word_chunk.py` 补至少一个边界测试，然后再提交本周代码。

## 建议改进项

1. 在 `trackers/weekly-log.csv` 的第一周行补上实际投入、成果链接和复盘备注。
2. 把 `first-CLI` 的 README 改成项目型 README：功能、运行命令、测试命令、当前限制。
3. 下周不要急着做语义分块；先把 CLI 输入校验、输出路径、README、pytest 做稳。

## 下周建议任务

- 任务 1：修 `first-CLI` 的 CLI 边界：文件不存在、`chunk_size <= 0`、输出路径。
- 任务 2：补齐 `word_chunk` 测试，并把 pytest 命令写进 README。
- 任务 3：完成一次干净提交：只提交周复盘、日志归档、测试补充，不混入无关文档改动。

以后再升级：

- 支持按句子或段落 chunk。
- 用 `pytest.ini` 或包结构替代测试里的 `sys.path.append(...)`。
- 给 CLI 主流程写集成测试。

## 是否建议上传 GitHub

- [ ] 可以上传
- [x] 修改后再上传
- [ ] 暂不上传，需要重做关键部分

说明：本周整体成果值得上传，但当前工作区还有日志移动、旧文件删除、资源文档修改、脚本目录等混杂状态。建议先补周复盘和 `word_chunk` 边界测试，再分批提交。

