# 每日作业批改

日期：2026-06-01

归档文件：

- `homework-reviews/2026-06-01-review.md`

学习阶段：

- [ ] 入门练习：能运行、能解释、能修明显错误
- [x] 小步改进：开始拆函数、减少重复、补边界情况
- [x] 工程化练习：函数、测试、CLI、README

## 总评

评分：72/100
等级：勉强合格，明天必须补齐作业证据

一句话评价：今天修掉了昨天 `chunk_size=0` 会崩的问题，测试也从昨天的 12 个增加到 14 个并全部通过；但第 2 周 Day 1 的核心要求是函数拆分、类型标注和 3 个小练习，今天的日志和代码证据没有完整覆盖这些要求。

## 分项评分

- 当前阶段完成度：18/30
- 正确性和可运行性：23/25
- 代码可读性：10/15
- 学习日志和证据：13/20
- 明日可执行性：8/10

## 必改项

1. 补齐 Day 1 的核心产出：至少给 2 个函数加参数和返回值类型标注，并在学习日志里解释其中 1 个函数的输入、处理、输出。
2. 把今天的 Git 证据整理清楚：`python-foundations/README.md` 现在仍是未跟踪文件，而提交 `81e31fb python-foundations README.md` 实际修改的是 `daily-homework/README.md`，两者不一致。

## 建议改进项

1. `main.py` 现在能拦截 `chunk_size <= 0`，但 `Wordchunk.text_chunk()` 本身仍没有防御这个输入。后面如果别人直接实例化 `Wordchunk(..., 0)`，仍会报 `range() arg 3 must not be zero`。
2. `main.py` 的文件检查还会打印 `0`，这是调试残留，建议删除。
3. `python-foundations/README.md` 目前只有 4 行，适合作为起点，但还需要加入运行命令、测试命令和目录说明，才算能帮助别人复现。

## 代码反馈

涉及文件：

- `python-foundations/first-CLI/src/main.py`
- `python-foundations/first-CLI/src/word_chunk.py`
- `python-foundations/first-CLI/tests/test_text_status.py`
- `python-foundations/README.md`
- `learning-log/second-week-log/2026-06-01学习日志：.md`

具体问题：

- `main.py` 新增的 `chunk_size <= 0` 检查有效，CLI 输入 `--chunk_size 0` 不再 traceback，而是输出 `Chunk size is invalid` 并退出。
- 当前代码里基本还没有函数类型标注。今天作业明确要求“给函数参数和返回值加类型标注”，这部分没有形成证据。
- 今天日志写“新增了 test_text_status.py 中的测试样例”，但这个文件的新增测试已经在当前 `origin/main` 上；今天真正未提交的代码改动主要是 `main.py` 的 `chunk_size` 检查。
- `python-foundations/README.md` 是有效方向，但还没有被 Git 跟踪。

当前阶段不要求：

- 不要求把所有旧代码一次性改成完美包结构。
- 不要求今天给 CLI 所有异常都写测试。
- 不要求 README 写成作品集最终版。

## 测试结果

我实际运行了：

```bash
python-foundations/first-CLI/.venv/bin/python -m pytest python-foundations/first-CLI/tests -q
```

结果：

```text
8 passed in 0.02s
```

我实际运行了：

```bash
python-foundations/practice/.venv/bin/python -m pytest python-foundations/practice/tests -q
```

结果：

```text
6 passed in 0.01s
```

我实际运行了：

```bash
python-foundations/practice/.venv/bin/python -m pytest python-foundations -q
```

结果：

```text
14 passed in 0.03s
```

我还实际跑了 CLI：

```text
正常输入可以运行成功。
--chunk_size 0 会输出 Chunk size is invalid，并以非 0 状态退出。
```

## 学习日志反馈

- 日志能说明今天做了 README 和测试，但太短，不能支撑 Day 1 的核心目标。
- 今天日志没有解释一个函数的输入、处理、输出；这是作业要求第 5 项。
- “测试结果：无”不准确。今天实际全量测试是 `14 passed`，建议以后把命令和输出写进去。
- README 和 module 笔记是有价值的自由探索/工程基础补充，但它们不能替代今天的函数拆分和类型标注任务。

## 明日修改任务

- 任务 1：给 `TextStats` 或 `word_freq_cnt` 至少 2 个函数补类型标注，例如参数类型和返回值类型。
- 任务 2：在学习日志里补一段函数解释：输入是什么、处理过程是什么、输出是什么。
- 任务 3：决定是否提交 `python-foundations/README.md`；如果要提交，补上最小运行命令和测试命令。

以后再升级：

- 把 `main.py` 的顶层执行逻辑拆进 `main()`，并使用 `if __name__ == "__main__": main()`。
- 把 `Wordchunk.text_chunk()` 自身也改成能处理 `chunk_size <= 0`。
- 清理工作区中旧文件移动/删除后的大量 Git 状态，提交前不要直接 `git add .`。

## 是否建议上传 GitHub

- [ ] 可以上传
- [x] 修改后再上传
- [ ] 暂不上传，需要重做关键部分

说明：测试已经全绿，且当前 `main` 与 `origin/main` 同步；但今天仍有未提交的 `main.py` 改动、未跟踪的 README/日志/笔记，以及 Day 1 关键要求未补齐。建议先补类型标注和日志说明，再提交。

## 复查结果

复查日期：

- [ ] 通过
- [ ] 未通过

复查备注：
