# 每日作业批改

日期：2026-05-30

归档文件：

- `homework-reviews/2026-05-30-review.md`

学习阶段：

- [ ] 入门练习：能运行、能解释、能修明显错误
- [x] 小步改进：开始拆函数、减少重复、补边界情况
- [x] 工程化练习：函数、测试、CLI、README

## 总评

评分：70/100
等级：勉强合格，明天必须先修关键问题

一句话评价：今天把 `word_chunk` 和 `text_status` 往类和 `dataclass` 方向推进了，这是有效进步；但代码状态还没有闭环，`main.py` 仍然使用旧函数导入，`TextStats` 没有测试，整体 pytest 还有 1 个失败。

## 分项评分

- 当前阶段完成度：19/30
- 正确性和可运行性：16/25
- 代码可读性：10/15
- 学习日志和证据：16/20
- 明日可执行性：9/10

## 必改项

1. 修复 `python-foundations/first-CLI/src/main.py` 的旧导入和旧调用方式。现在 `text_status.py` 已经改成 `TextStats` 类，`word_chunk.py` 已经改成 `Wordchunk` 类，但 `main.py` 仍在导入 `text_chunk`、`line_check`、`word_check` 等旧函数。
2. 补齐 `TextStats` 和 `Wordchunk` 的测试闭环：至少给 `TextStats` 一个正常输入测试，给 `Wordchunk` 一个边界输入测试，例如空文件、chunk_size 为 0 或 chunk_size 大于文本长度。

## 建议改进项

1. 明天可以先统一 `dedupe.py` 的空列表行为：要么空列表返回 `[]`，要么测试改成期待 `ValueError`，不要代码和测试互相矛盾。
2. `groups_words.py` 现在只 `print(groups)`，没有 `return groups`，后面要测试它时会卡住；建议改成返回字典。
3. `sort_records.py` 仍是脚本式演示代码，后面可以拆成 `sort_records(students)` 函数再补测试。

## 代码反馈

涉及文件：

- `python-foundations/first-CLI/src/text_status.py`
- `python-foundations/first-CLI/src/word_chunk.py`
- `python-foundations/first-CLI/src/main.py`
- `python-foundations/first-CLI/tests/test_word_chunk.py`
- `python-foundations/practice/src/dedupe.py`
- `python-foundations/practice/tests/test_dedupe.py`

具体问题：

- `TextStats` 的类化方向是对的，`space_check`、`line_check`、`word_check`、`digit_check` 都适合做实例方法。但当前没有对应测试文件；今天已有的是 `test_word_chunk.py`，它覆盖了 `Wordchunk` 的基础场景。
- `Wordchunk.text_chunk()` 对负数 chunk_size 做了处理，但没有处理 `chunk_size == 0`。`range(0, len(text), 0)` 会直接报错。
- `main.py` 还没适配类化后的接口，所以 CLI 主入口当前不可用。
- `dedupe.py` 对空列表抛 `ValueError`，但 `test_dedupe.py` 期待空列表返回 `[]`，这是今天实际测试失败的原因。

当前阶段不要求：

- 不要求今天把所有 practice 文件都函数化。
- 不要求今天完成复杂异常体系。
- 不要求今天把 CLI 做成可安装包。

## 测试结果

我实际运行了：

```bash
python-foundations/first-CLI/.venv/bin/python -m pytest python-foundations/first-CLI/tests -q
```

结果：

```text
5 passed in 0.03s
```

我实际运行了：

```bash
python-foundations/practice/.venv/bin/python -m pytest python-foundations/practice/tests -q
```

结果：

```text
1 failed, 2 passed
```

失败点：

```text
python-foundations/practice/tests/test_dedupe.py::test_remove_duplicates
ValueError: 没有具体值
```

我实际运行了：

```bash
python-foundations/practice/.venv/bin/python -m pytest python-foundations -q
```

结果：

```text
1 failed, 7 passed
```

## 学习日志反馈

- 日志能说明今天主线：学习 class / dataclass，并把旧函数改写成类，这是有效记录。
- “遇到的问题”和“如何解决”写得真实，尤其是对象创建和方法调用这一点。
- 证据部分基本能对应到 `test_word_chunk.py`，但“测试结果 pass”需要写清楚范围：`first-CLI` 是 pass，全量 `python-foundations` 不是全 pass。
- OOP 笔记覆盖了 tuple、dict、class、object、method、raise、`__str__`，但还有模板空段落和几段语法错误示例。作为学习笔记可以保留，但明天最好补一句“我今天真正理解了什么”。

## 明日修改任务

- 任务 1：先修 `main.py`，让 CLI 使用 `Wordchunk(...).text_chunk()` 和 `TextStats(...).line_check()` 这类新调用方式。
- 任务 2：补 `test_text_status.py`，至少测试正常文本的行数、词数、数字数；同时给 `Wordchunk` 增加一个边界测试。
- 任务 3：统一 `dedupe.py` 空列表行为，让 `practice` 测试重新变成全绿。

以后再升级：

- 把 `groups_words.py`、`sort_records.py`、`two_sum.py` 都改成“函数 + 测试”的形式。
- 给文件读取统一加 `encoding="utf-8"`。
- 清理根目录旧文件删除和移动后的 Git 状态，避免提交时混入无关改动。

## 是否建议上传 GitHub

- [ ] 可以上传
- [x] 修改后再上传
- [ ] 暂不上传，需要重做关键部分

说明：当前本地 `main` 已经 ahead 3，但工作区还有大量删除、移动、未跟踪文件和 1 个测试失败。建议先修必改项并确认全量测试通过，再分组提交或推送；不要直接 `git add .`。

## 复查结果

复查日期：

- [ ] 通过
- [ ] 未通过

复查备注：
