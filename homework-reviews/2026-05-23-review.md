# 每日作业批改

日期：2026-05-23

归档文件：

- `homework-reviews/2026-05-23-review.md`

学习阶段：

- [x] 入门练习：能运行、能解释、能修明显错误
- [ ] 小步改进：开始拆函数、减少重复、补边界情况
- [ ] 工程化练习：函数、测试、CLI、README

## 总评

评分：76/100  
等级：合格，有明确必改项

一句话评价：

今天的核心学习目标完成了：你把 dict、items、sorted、lambda、切片 `[:10]` 用到了词频 Top 10 任务里，也能在日志里说出自己卡在哪里、怎么解决；但代码现在依赖运行目录，从仓库根目录运行会失败，明天必须先修这个问题。

## 分项评分

- 当前阶段完成度：24/30
- 正确性和可运行性：18/25
- 代码可读性：11/15
- 学习日志和证据：17/20
- 明日可执行性：6/10

## 必改项

最多 1-2 条，只写当前阶段必须修的内容。

1. 修正 `python-foundations/word_frequency_count.py` 对 `01.txt` 的运行目录依赖。现在从仓库根目录运行 `python3 python-foundations/word_frequency_count.py` 会报 `FileNotFoundError: '01.txt'`，因为第 18 行写死了相对路径。明天优先改成接收输入路径，或者用脚本所在目录定位样例文件。
2. 明日计划不能只写“按照明日的作业规划安排”。需要具体到文件和动作，例如：`修改 word_frequency_count.py，让它从命令行参数读取文件路径，并保留 python-foundations/01.txt 作为样例输入。`

## 建议改进项

最多 1-3 条，分清楚“明天可以做”和“以后再做”。

1. 明天可以做：让 dict 版本也输出 `(word, count)`，不要只输出词列表。这样你才能确认排序不只是排出了词，还保留了频率证据。
2. 明天可以做：把第 36 行 `from collections import Counter` 移到文件顶部，旧的整段注释代码可以先删掉或移到学习日志里，代码文件会更干净。
3. 以后再做：再考虑去标点、支持中文分词、pytest、CLI 参数，这些属于后续工程化，不是今天必须完成。

## 代码反馈

涉及文件：

- `python-foundations/word_frequency_count.py`

具体问题：

- 第 18 行 `fname = '01.txt'` 让脚本只能在 `python-foundations/` 目录内运行。从仓库根目录运行失败，这会影响别人复现你的作业。
- 第 31-32 行 dict 版本只打印 Top 10 单词列表，没有打印 count。今天的任务是词频统计，建议保留频率数字。
- 第 51-52 行在文件顶层直接运行函数。当前阶段可以接受，但下一步最好加 `if __name__ == "__main__":`，这样以后导入函数写测试时不会自动执行。

当前阶段不要求：

- 不要求今天必须写 pytest。
- 不要求今天必须做完整 CLI。
- 不要求处理复杂标点、中文分词和停用词。

## 学习日志反馈

- 优点：日志写清楚了今天卡住的两个点：如何按 value 排序、如何只截前 10 个；解决方法也对应到了 `items()`、`sorted`、`lambda`、`[:10]`，这是有效学习记录。
- 问题：产出证据只记录了词列表，没有记录带 count 的完整输出；Commit 一栏为空；明天计划太泛。
- 建议：以后日志里的“测试结果”尽量贴完整命令和输出，例如 `cd python-foundations && python3 word_frequency_count.py`，这样复查时能判断运行环境。

## 明日修改任务

- 任务 1：修改 `python-foundations/word_frequency_count.py`，解决 `01.txt` 的路径问题。最低要求：从仓库根目录和 `python-foundations/` 目录运行都能成功，或 README/日志里明确写出唯一正确运行命令。
- 任务 2：让 dict 版本输出 Top 10 的单词和频率，例如 `plum: 19`，并在学习日志里解释 `sorted(word_cnt.items(), key=lambda item: item[1], reverse=True)` 每一部分是什么意思。

以后再升级：

- 给 `word_freq_cnt1` 和 `word_freq_cnt` 返回数据，而不是只 `print`。
- 增加一个最小 pytest 测试：输入一小段固定文本，断言 Top 3 结果正确。

## 是否建议上传 GitHub

- [ ] 可以上传
- [x] 修改后再上传
- [ ] 暂不上传，需要重做关键部分

## 复查结果

复查日期：

- [ ] 通过
- [ ] 未通过

复查备注：

