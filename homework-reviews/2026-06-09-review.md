# 每日作业批改

日期：2026-06-09

归档文件：

- `homework-reviews/2026-06-09-review.md`

批改对象：

- `daily-homework/week-03/day-06.md`
- `python-foundations/first-CLI/tests/test_createjson.py`
- `python-foundations/first-CLI/tests/test_summarize.py`
- `learning-log/third-week-log/2026-06-09日志.md`

学习阶段：

- [ ] 入门练习：能运行、能解释、能修明显错误
- [ ] 小步改进：开始拆函数、减少重复、补边界情况
- [x] 工程化练习：函数、测试、CLI、README

## 总评

评分：86/100
等级：合格

一句话评价：今天的核心目标完成得比较扎实：`summarize_text`、JSON 保存和读回、空输入、父目录创建都已经有测试覆盖，pytest 也通过；主要不足是学习日志里对 `create_json` 参数的解释还不够准确，且没有整理提交给 Codex 的具体问题。

## 分项评分

- 当前阶段完成度：27/30
- 正确性和可运行性：25/25
- 代码可读性：12/15
- 学习日志和证据：16/20
- 明日可执行性：6/10

## 必改项

最多 1-2 条，只写当前阶段必须修的内容。

1. 在学习日志里补 1-3 个提交给 Codex 的具体问题，例如：`create_json` 的参数名应该叫 `data` 还是 `json_result`？`tmp_path` 测试是否需要断言 JSON 内容？是否需要给 CLI 主入口写 subprocess 测试？

## 建议改进项

最多 1-3 条，分清楚“明天可以做”和“以后再做”。

1. 明天可以把 `test_createjson.py` 改名为 `test_create_json.py`，命名更贴近模块名和 pytest 常见风格。
2. 明天可以在学习日志里纠正一句理解：`create_json(data, output_path)` 接收的是“要写入的 Python 数据”和“输出路径”，不是先读取文件再传字符串。
3. 以后再给 `src/main.py` 写一个 subprocess 级别测试，确认 CLI 入口真的会执行。

## 代码反馈

涉及文件：

- `python-foundations/first-CLI/tests/test_createjson.py`
- `python-foundations/first-CLI/tests/test_summarize.py`

具体问题：

- `test_create_json_file` 覆盖了 `create_json()` 的返回路径和文件是否创建。
- `test_create_json_content` 覆盖了写入后用 `json.load()` 读回，能防止 Day5 那种 JSON schema 退回错误。
- `test_create_json_creates_parent_directory` 覆盖了 `tmp_path / "nested" / "result.json"`，能验证父目录自动创建。
- `test_summarize_empty_text` 覆盖了空输入，符合 Day6 的第三条要求。
- `test_summarize.py` 还保留了 brief、bullets、非法 mode 三个测试，覆盖面比昨天更完整。
- `test_createjson.py` 顶部 import 和函数之间缺少一些空行，属于风格问题，不影响当前阶段。

验证结果：

- `./.venv/bin/python -m pytest tests -q`：`15 passed in 0.03s`

当前阶段不要求：

- 不要求测试真实 LLM。
- 不要求测试完整 CLI 所有参数组合。
- 不要求引入 mock 或 monkeypatch。
- 不要求把测试覆盖率做到完整。

## 学习日志反馈

- 学习日志记录了今天新增 `create_json` 测试、补充 `summarize_text` 空输入测试，并记录了 `15 passed`，证据比昨天完整。
- 卡点写得真实，尤其是 `tmp_path` 不熟和空输入应该测在哪个函数上，这两个问题都和今天目标直接相关。
- 但“应该先读取文件，生成字符串的变量，再传入 create_json”这句话不准确。当前 `create_json(data, output_path)` 的职责是保存 JSON 数据，不负责读取源文件；读取文件和生成 summary 是 `main.py` 或更上层逻辑的职责。
- 明天第一步写得还是偏泛：`根据 Codex 批改意见修改今日作业`。更好的写法是：打开 `learning-log/third-week-log/2026-06-09日志.md`，补充 1-3 个 Codex 批改问题，并修正 `create_json` 参数理解。

## 明日修改任务

- 任务 1：补学习日志里的“提交给 Codex 的具体问题”，至少写 2 个，必须指向测试或函数设计。
- 任务 2：修正学习日志里对 `create_json` 参数的理解：它接收 `data` 和 `output_path`，不负责读取输入文件。

以后再升级：

- 给 `main.py` 写 subprocess 测试，验证 `python src/main.py test0.txt --summary` 和 `--createjson`。
- 把 `test_createjson.py` 改名为 `test_create_json.py`。
- 给 `create_json()` 增加一个返回值断言加内容断言合并测试，减少重复。

## 是否建议上传 GitHub

- [x] 可以上传
- [ ] 修改后再上传
- [ ] 暂不上传，需要重做关键部分

说明：今日 Day6 核心功能和证据已经达标，可以上传。上传前如果想更完整，先补学习日志里的具体问题和 `create_json` 参数理解。

## 复查结果

复查日期：

- [ ] 通过
- [ ] 未通过

复查备注：
