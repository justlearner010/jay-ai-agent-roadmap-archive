# 2026-05-22 作业批改

归档文件：

- `homework-reviews/2026-05-22-review.md`

## 学习阶段

- [ ] 入门练习：能运行、能解释、能修明显错误
- [x] 小步改进：开始拆函数、减少重复、补边界情况
- [ ] 工程化练习：函数、测试、CLI、README

## 总评

评分：78/100  
等级：合格，但需要先整理作业证据

一句话评价：今天比昨天有进步，尤其是 `text_status.py` 开始拆函数，`word_frequency_count.py` 开始用字典统计词频；但两个代码文件现在还在 `/Users/jay/Documents/python_practice/`，没有放进当前 GitHub 仓库，学习日志里的截图链接也不是仓库内可用路径，所以“作业闭环”还没完成。

## 分项评分

- 当前阶段完成度：25/30
- 正确性和可运行性：18/25
- 代码可读性：11/15
- 学习日志和证据：15/20
- 明日可执行性：9/10

## 必改项

1. 先把今天两个作业文件放进仓库的 `python-foundations/`，否则 GitHub 上看不到今天的代码证据：
   - `/Users/jay/Documents/python_practice/text_status.py`
   - `/Users/jay/Documents/python_practice/word_frequency_count.py`
2. `word_frequency_count.py` 现在不是“出现频率最高的 10 个词”，只是按出现顺序打印前 10 个词。明天优先把它改成按次数从高到低排序。

## 建议改进项

1. `text_status.py` 里 `space_check()` 的输出文字写错了，现在打印的是“数字的个数”，应改成“空白字符的个数”。
2. `word_frequency_count.py` 先不用追求复杂清洗，但可以最小改一下：把 `banana,` 这种标点去掉，避免和 `banana` 被当成两个词。
3. 学习日志里的截图目前是 Obsidian 风格链接：`![[Pasted image...]]`，GitHub 不能正常显示。建议把截图移动到 `learning-log/assets/`，再用 Markdown 图片路径引用。

## 代码反馈

涉及文件：

- `/Users/jay/Documents/python_practice/text_status.py`
- `/Users/jay/Documents/python_practice/word_frequency_count.py`

做得好的地方：

- `text_status.py` 已经从纯脚本进步到多个小函数。
- 使用了 `with open(...) as file`，这比手动 `close()` 更稳。
- `word_frequency_count.py` 用了字典计数，这是哈希表思想的入门练习，方向对。
- 两个文件都通过了 `py_compile`，语法没有问题。

具体问题：

- `word_frequency_count.py` 的 `word_cnt` 是全局变量，当前阶段可以接受，但之后要放进函数里。
- `list(word_cnt.items())[:10]` 不代表最高频 Top 10。
- 没有处理标点，所以 `banana` 和 `banana,` 会被分开统计。
- `text_status.py` 的函数现在只 `print()`，暂时可以；后面再学返回值。

当前阶段不要求：

- 不要求 pytest。
- 不要求 CLI 参数。
- 不要求一次写成作品集级别代码。
- 不要求今天就完全工程化。

## 学习日志反馈

日志写得比昨天更清楚，尤其是写到了 `.strip()` 和 `.split()`，这很好。

需要补强的是“证据准确性”：

- 代码文件写了，但文件不在当前仓库。
- 测试结果只写“成功”，最好写你运行了什么命令。
- Commit 为空。
- 截图路径不是 GitHub 可识别路径。

## 明日修改任务

- 任务 1：把今天两个 `.py` 文件移动或复制到 `python-foundations/`，并修改学习日志里的代码文件路径。
- 任务 2：把 `word_frequency_count.py` 改成真正的 Top 10：先排序，再打印。

以后再升级：

- 把 `word_frequency_count.py` 改成函数。
- 写一个小样本文本，记录运行结果。
- 学会 pytest 后再补测试。

## 是否建议上传 GitHub

- [ ] 可以上传
- [x] 修改后再上传
- [ ] 暂不上传，需要重做关键部分

建议你先完成“文件放进仓库 + 修 Top 10 排序 + 修截图路径”这三件事，再上传。

## 复查结果

复查日期：

- [ ] 通过
- [ ] 未通过

复查备注：

