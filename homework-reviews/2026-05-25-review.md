# 2026-05-25 作业批改

日期：2026-05-25

归档文件：

- `homework-reviews/2026-05-25-review.md`

学习阶段：

- [ ] 入门练习：能运行、能解释、能修明显错误
- [x] 小步改进：开始拆函数、减少重复、补边界情况
- [x] 工程化练习：函数、测试、CLI、README

说明：今天已经把前几天的文本统计、词频统计、文本分块和 JSON 输出合并成了一个 CLI 项目，属于工程化练习的第一步。当前重点不是做复杂功能，而是让 CLI 的输入、输出和 README 变得可靠。

## 总评

评分：84/100
等级：良好，修改 1-2 个关键点后适合提交

一句话评价：

今天的 `first-CLI` 是有效进步：你已经用 `argparse` 建立了真正的命令行入口，并把多个脚本拆成模块；主要问题是异常输入会直接 traceback，输出文件路径也不够明确。

## 分项评分

- 当前阶段完成度：27/30
- 正确性和可运行性：20/25
- 代码可读性：11/15
- 学习日志和证据：17/20
- 明日可执行性：9/10

## 必改项

最多 1-2 条，只写当前阶段必须修的内容。

1. 给 CLI 加最基本的错误提示：`--chunk_size 0` 现在会触发 `ValueError: range() arg 3 must not be zero`，文件不存在会触发 `FileNotFoundError`。明天先让程序输出人能看懂的错误，而不是 Python traceback。
2. 明确 JSON 输出路径：`create_json.py` 现在写死生成当前工作目录下的 `test.json`。作为 CLI 项目，至少要在 README 写清楚从哪个目录运行、输出到哪里；更好的是支持 `--output test.json`。

## 建议改进项

最多 1-3 条，分清楚“明天可以做”和“以后再做”。

1. 明天可以把 README 文件名从 `README.md.md` 改成 `README.md`，并补一条真实命令，例如 `python3 main.py test1.txt --chunk_size 100`。
2. 明天可以删掉 `word_chunk.py`、`word_freq.py` 里大段旧版本注释，只保留当前可运行版本。旧代码可以放到学习日志，不必堆在项目文件里。
3. 以后再做 pytest、安装入口、语义分块和 overlap；这些不应该压到明天的必改任务里。

## 代码反馈

涉及文件：

- `python-foundations/first-CLI/main.py`
- `python-foundations/first-CLI/word_chunk.py`
- `python-foundations/first-CLI/create_json.py`
- `python-foundations/first-CLI/word_freq.py`
- `python-foundations/first-CLI/text_status.py`
- `python-foundations/first-CLI/README.md.md`

具体问题：

- 主流程可以运行。我实际执行了 `python3 main.py test1.txt --chunk_size 10`，程序成功生成 JSON，并输出 Top 10 词频、行数、单词数、空格数和数字数。
- `main.py` 已经正确使用 `argparse`，这是今天最重要的进步；`--help` 也能自动生成。
- `--chunk_size 0` 没有被拦截，导致 `range()` 报错。这个问题非常适合明天用 `parser.error("--chunk_size must be greater than 0")` 修掉。
- 文件不存在时没有友好提示。现在用户输入错文件名会看到完整 traceback，不像一个 CLI 工具。
- `create_json(chunks)` 写死输出 `test.json`，而且输出位置取决于运行命令时所在目录。这个行为容易让你以为文件丢了。
- `word_chunk.py` 里变量名仍然是 `chucks`，建议改成 `chunks`。
- 文件读取建议统一加 `encoding="utf-8"`，以后处理中文文本会更稳。
- README 目前仍有模板残留，比如“前端、后端、向量库、数据库”等，这些和当前 Python CLI 项目关系不大，可以删掉或改成 CLI 项目的模块说明。
- README 里的图片和 demo 仍是 Obsidian 语法，GitHub 上不会正常显示。

当前阶段不要求：

- 不要求今天做成可安装 Python package。
- 不要求今天写 pytest。
- 不要求今天实现语义分块。
- 不要求今天接入 LLM、向量库或数据库。
- 不要求今天把 README 写成作品集最终版。

## 学习日志反馈

- 今天日志写清楚了目标：完成第一个 CLI 工具。
- 你能说出今天的关键学习点：`argparse`、Python 项目拆分、把 `sys.argv` 改成更正式的参数解析。
- “测试结果：成功”仍然偏笼统。以后建议直接写实际命令和关键输出，例如：

```bash
cd python-foundations/first-CLI
python3 main.py test1.txt --chunk_size 10
```

- `argparse` 笔记是有效的，但里面还有空模板段落；下次提交前可以删掉空的“常见错误 / 我今天的理解 / 后续问题”，或者至少写 1 句自己的理解。
- 今天还没有 commit，这和你说明一致。当前不建议直接提交整个仓库，因为工作区里不只有 `first-CLI`，还有很多历史文件修改、删除和新文件。

## 明日修改任务

- 任务 1：给 `main.py` 加输入保护：`chunk_size <= 0` 时给出用法错误；文件不存在时给出清楚提示。
- 任务 2：让 JSON 输出路径清楚。最低要求是在 README 写明输出到 `test.json`；更推荐新增 `--output` 参数。

以后再升级：

- 把核心函数加上简单 pytest。
- 支持按段落或句子切分。
- 给 chunk 加 overlap。
- 把词频统计中的标点清理做得更准确。

## 是否建议上传 GitHub

- [ ] 可以上传
- [x] 修改后再上传
- [ ] 暂不上传，需要重做关键部分

说明：`first-CLI` 本身方向正确，修完两个必改项后可以提交。但不要直接 `git add .`，当前仓库还有大量和今天作业无关或需要确认的未提交改动。建议只选择今天作业相关文件提交：`python-foundations/first-CLI/`、`learning-log/2026-05-25学习日志.md`、`notes/2026-05-25笔记：argparse.md`、`notes/2026-05-25笔记：Collection Counter.md`、本批改文件。

## 复查结果

复查日期：

- [ ] 通过
- [ ] 未通过

复查备注：

