# 每日作业批改模板

日期：2026-06-13

归档文件：

- `homework-reviews/2026-06-13-review.md`

归档要求：

- 每次作业批改结果都必须保存到 `homework-reviews/`。
- 文件名使用 `YYYY-MM-DD-review.md`。
- 如果当天有复查结果，可以追加到同一个文件的“复查结果”部分。

学习阶段：

- [ ] 入门练习：能运行、能解释、能修明显错误
- [ ] 小步改进：开始拆函数、减少重复、补边界情况
- [x] 工程化练习：函数、测试、CLI、README

## 总评

评分：58/100
等级：需要重做关键部分

一句话评价：

今天已经开始把 LLM 调用包装出来，但测试和函数签名没有对齐，导致 `test_summarize.py` 和全量测试失败；另外还没有真正完成 Day 3 要求的失败路径测试和 monkeypatch/mock 替换。

## 分项评分

- 当前阶段完成度：16/30
- 正确性和可运行性：8/25
- 代码可读性：10/15
- 学习日志和证据：14/20
- 明日可执行性：10/10

## 必改项

最多 1-2 条，只写当前阶段必须修的内容。

1. 修正 `summarize_with_llm()` 和测试的接口不一致问题：要么给 `summarize_with_llm(text, llm_func=call_llm)` 增加参数，要么改成 `import summarize` 后用 `monkeypatch.setattr(summarize, "call_llm", fake_call_llm)`。
2. 补一个失败路径测试：让 fake LLM 抛出 `RuntimeError`，确认 `summarize_with_llm("hello")` 返回 `"LLM summary failed."` 或你明确设计的错误处理结果。

## 建议改进项

最多 1-3 条，分清楚“明天可以做”和“以后再做”。

1. 明天可以做：清理 `python-foundations/first-CLI/tests/test_summarize.py` 第 1-30 行旧注释代码，这个问题已经连续两天出现。
2. 明天可以做：在 `learning-log/fourth-week-log/2026-06-13日志.md` 里补上实际 pytest 命令和失败结果，不要只留空的“测试结果”。
3. 以后再做：再学习 `unittest.mock.patch`；当前阶段先用依赖注入或 pytest `monkeypatch` 中的一种写通即可。

## 代码反馈

涉及文件：

- `python-foundations/first-CLI/src/summarize.py`
- `python-foundations/first-CLI/tests/test_summarize.py`

具体问题：

- `summarize.py` 第 2-3 行新增了 `call_llm(text)`，这符合“把 LLM 调用包装成函数”的方向。
- `summarize.py` 第 6-13 行新增了 `summarize_with_llm(text)`，并处理了空文本和 `RuntimeError`，方向也对。
- 但 `test_summarize.py` 第 120 行调用了 `summarize_with_llm("hello", llm_func=fake_llm)`，而 `summarize_with_llm()` 当前定义不接受 `llm_func` 参数，所以测试失败。
- `test_summarize.py` 第 116-120 行现在只覆盖成功路径，没有覆盖 Day 3 要求的“失败返回时的处理逻辑”。
- 当前测试没有使用 `monkeypatch` 或 `unittest.mock`。如果你选择依赖注入写法，也可以不用 monkeypatch，但需要在学习日志里说明“我用传函数参数替代 monkeypatch 来隔离外部依赖”。

当前阶段不要求：

- 不要求接真实 OpenAI 或其他 LLM API。
- 不要求引入新的依赖。
- 不要求同时掌握 `monkeypatch` 和 `unittest.mock` 两种方式。

## 学习日志反馈

- 学习日志写清楚了今天目标：新增 LLM 包装函数、写成功测试、写失败测试。
- 实际代码只完成了 LLM 包装函数和一个成功测试的雏形；失败测试还没有出现。
- “今天卡点：不知道什么是 monkey patch”是准确卡点，但需要补一句自己的理解：monkeypatch 是在测试里临时替换某个函数或属性，让测试不依赖真实外部调用。
- “测试结果”为空，但本次批改实际复跑结果是失败：
  - `./.venv/bin/python -m pytest tests/test_summarize.py`
  - 结果：`1 failed, 10 passed, 1 xfailed`
  - 失败原因：`TypeError: summarize_with_llm() got an unexpected keyword argument 'llm_func'`
- 全量测试也失败：
  - `./.venv/bin/python -m pytest`
  - 结果：`1 failed, 27 passed, 1 xfailed`

## 明日修改任务

- 任务 1：先修 `python-foundations/first-CLI/src/summarize.py`，把函数签名改成 `def summarize_with_llm(text, llm_func=call_llm):`，并在函数内部调用 `llm_func(text)`。
- 任务 2：在 `python-foundations/first-CLI/tests/test_summarize.py` 增加失败路径测试：fake LLM 抛出 `RuntimeError`，断言返回 `"LLM summary failed."`。

以后再升级：

- 用 `monkeypatch.setattr()` 写同一组测试，理解 monkeypatch 和依赖注入的差异。
- 给 LLM 调用错误增加日志记录。

## 是否建议上传 GitHub

- [ ] 可以上传
- [ ] 修改后再上传
- [x] 暂不上传，需要重做关键部分

## 复查结果

复查日期：2026-06-13

- [x] 通过
- [ ] 未通过

复查备注：

- 已修复 `summarize_with_llm()` 和测试接口不一致问题：函数签名改为 `summarize_with_llm(text, llm_func=call_llm)`，内部调用 `llm_func(text)`。
- 已补失败路径测试：fake LLM 抛出 `RuntimeError` 时返回 `"LLM summary failed."`。
- 已补空文本测试：`summarize_with_llm("")` 返回 `"No content to summarize."`。
- 已清理 `test_summarize.py` 顶部旧注释代码。
- 复查命令：`./.venv/bin/python -m pytest tests/test_summarize.py`，结果：`13 passed, 1 xfailed in 0.02s`。
- 全量测试：`./.venv/bin/python -m pytest`，结果：`30 passed, 1 xfailed in 0.04s`。
