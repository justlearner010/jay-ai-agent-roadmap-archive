### 主题：Python I O文件读取

日期：2026-05-22

## 一句话解释

这是在讲如何用python进行文件读取


## 为什么重要

说明它和当前项目、AI Agent、RAG、后端工程或科班基础有什么关系。
python的I/O读取是我们在构建我们的Agent项目中需要用到的 重要知识点，负责让AI读取文件

## 核心要点

- `with`
`with` 可以在打开时最后也关闭文件，比`close`更加可靠
- `open`
- `close`
- `csv`
- `read()`
- `readline()`
- `readlines()`
- `rstrip()`
- `sorted()`
`rstrip()`可以用于去除字符串最后的换行符`/n` 可以让读取单行的`readlines()`不重复产生多的一个空行

## 代码例子

### `with open`

```python
# 放一个最小可运行例子
name = input("what's your name")
with open("names.txt","a") as file:
	file.write(f"{name}\n")
	lines = file.readlines()
	
```

### `rstrip()`

```python

for line in lines :
	print("hello,",line.rstrip())
```
### ``
### `sorted()`

```python
names = []

with open("file_io/names.txt","r") as file:

for line in file:

names.append(line.rstrip())

print(names)

for name in sorted(names):

print(f"Hello,{name}")
```

``` python
# 运行结果
'James', 'Bob', 'Kobe', 'John', 'Jay', 'Brown']
Hello,Bob
Hello,Brown
Hello,James
Hello,Jay
Hello,John
Hello,Kobe
```


`sorted(iterable,/,*,key=None,reverse=False)`:sorted的默认顺序


### `csv`
comma separate values :用于储存表格数据

csv文件可以通过读取,号来将数据分割成不同的栏


# lambda function :anonymous function

 lambda is a no name function that is equivalent to the get_name function /get_nation function when you call it
 
but it can just run when the loop running 

lambda + parameter:(return value)student["name"]
## 常见错误

- 为什么使用单引号来引用name 和 nation： 在print里面已经有了双引号，python will confused




## 我今天的理解

写 3-5 句话，说明你现在如何理解它。

## 后续问题



